> **Nota de 2026-09-10 — las rutas de este registro cambiaron.** Los entregables se reorganizaron:
> lo que se envía a la revista vive ahora en `manuscript/04_submission_study/` y el corpus completo
> de doce documentos en `manuscript/02_others/`. Las entradas de abajo nombran las rutas que existían cuando se
> escribieron (`manuscript/journal/`, `manuscript/*.docx`) y se dejan tal cual, porque son el registro de lo
> que se hizo y cuándo. `config.SUBMISSION` y `config.CORPUS` son los nombres canónicos desde ahora.

# Decisions of the journal package (append-only; one heading per task)

## Task: DESIGN THE FOCUSED PAPER — lens C_policy_reader (2026-09-09)

Read before deciding: journal_guidelines.md and the archived PDF text; manuscript/article_sin_rett_en.pdf (65 pp) and prose_en.py; outputs/values_sin_rett.json (17,198 keys); supplementary_material.py (FIGURE_ORDER 54, TABLE_ORDER 125, FIGURE_INTRO); captions.json / titles.json of outputs/sin_rett/en and extra/; prose_methods_extended.py (methods_blocks, TABLE_KEYS M1–M10) and equations (33 EQUATIONS, 26 ESTIMATORS); references.bib (122 keys; the article cites 50: 28 in the core text, 22 only in [OPT] paragraphs); decision_log.md closing sections and memo_es.md section 6; figure_table_plan.md; reporting_checklist.md; analysis_plan.md; the five article plates viewed at reduced size.

Decisions (the plan itself is returned to the orchestrator as structured output; this note records what a builder must not re-open):

1. Body display items = six: Figure 1 fig1_dataflow, Figure 2 fig2_grd_core, Figure 3 fig3_rem_pathway, Figure 4 fig4_triangulation, Table 1 = row selection of T2_grd_core (25 of 46 rows), Table 2 = row selection of T3_rem_pathway (20 of 55 rows; 2021+ codes with n establishments in every cell). fig1_sources_coverage becomes the first supplementary figure; T4/T5/T6/T7/T8 leave the body (their numbers stay in the text, Figure 4 and the supplement). Row selection is a presentation filter declared in journal_config.py; cells must be byte-identical to the full CSV and the full tables are reprinted in the supplement.
2. Journal plate mode = a flag with default off (proposed env PLATE_JOURNAL=1 read in common.plate_resolve/save_fig or a journal=True argument): strip the title text after the lowercase panel letter, keep the bare bold letter, drop the variant stamp, hide top/right spines if any, write to outputs/<variant>/<lang>/journal/figures/ (NOT figures/ or extra/figures/, which supplementary_material._plate_stems scans and tests/test_supplementary_registry.py::test_no_plate_file_is_left_unregistered would flag). Re-render with PLATE_CHECK=strict.
3. Supplement (English only, one PDF, TOC, page numbers): section A applied methodology = prose_methods_extended.methods_blocks with all 33 equations and Tables S1–S10 (M1–M10, incl. the estimator map M5); section B analysis plan (English rendering of analysis_plan.md v1.0); section C STROBE + RECORD checklist re-mapped to the journal sections; section D supplementary results: 22 figures and 41 further tables (S11–S51) numbered by first citation from the journal article. Deep episode detail (EF1–EF10, E1–E12), spatial statistics beyond maps and regional summary (E42–E45, E47, E48, E50, E51), the E60–E81 complete data tables and the duplicate data tables (F3, F4, S5, T8 full, E40/E41/E15/E17 companion tables) go to the data-sharing repository, not the PDF.
4. References cut to exactly 30 (listed in the plan). Four dataset citations that only support appendix sensitivities (deis_rem20, ine_base2024, ine_censo2024, fonasa_aps) and wolter2007 move to the appendix reference list, which the journal numbers separately. cid2024, minsal2011, becerril2011, irarrazaval2023, santomauro2025, paula2011, fombonne2016, song2010 and the seven [OPT]-only literature keys are dropped; the sentences they supported are reworded or removed.
5. Word budget 4,600 (Introduction 420, Methods 1,400, Results 1,600, Discussion + Conclusion 1,180), measured with prose word_counts; Summary target ≤ 245 words.
6. Text format: mid-height decimal (23·4) in text and tables by a post-processing pass that must skip codes, DOIs, URLs, versions and dates; p values "<0·0001" where p < 0·0001, otherwise two significant figures; numbers one to ten in words except with units; no bold emphasis; en rule in ranges (already the corpus rule).
7. Standing rules re-affirmed for the journal version: administrative recognition, never prevalence; "episodes with documented F84"; F84-principal separate; no linkage/cascade/cross-source ratio; Law 21.545 context only; stocks and flows never on one axis; place of care vs residence warned; December primary/June sensitivity; cells < 5 suppressed; every printed number from values_sin_rett.json or a tidy table.

## Task B — Editorial plan for the sin_rett submission, methods-reviewer lens (`B_methods_reviewer`) — 2026-09-09

Read before deciding: `journal_guidelines.md` and the archived PDF text; `manuscript/article_sin_rett_en.pdf` (65 pages, 4,995 core words, 28 core / 50 total citation keys, 11 `[OPT]` paragraphs); `prose_en.py` (`article()`, `citation_keys()`, `main_citations()`, `word_counts()`); `outputs/values_sin_rett.json` (17,198 keys); `supplementary_material.FIGURE_ORDER` (54) and `TABLE_ORDER` (125); the four `captions.json` / `titles.json` of `outputs/sin_rett/en`; `prose_methods_extended.methods_blocks('sin_rett', V, 'en')` (136 blocks: M1–M7, 26 estimator headings, 33 equations, 10 methods tables); `equations.NUMBER` (33) and `ESTIMATORS` (26); `references.bib` (122 keys); `decision_log.md` (study rules, open decisions); `memo_es.md` §6; `figure_table_plan.md`; `analysis_plan.md`; `reporting_checklist.md`; the four body plates and four candidate supplementary plates read at reduced size. Baseline test battery: 300 tests collected (`pytest tests -q`), run recorded in this task's notes.

Decisions (each with its reason), all returned in the structured plan and summarised here so the builder can trace them:

1. **Body display items = 6** (4 figures, 2 tables). Kept: `fig1_dataflow` (STROBE 13 / RECORD 13.1 flow of records and the no-linkage rule); `fig2_grd_core` (primary estimand with its coding-depth-stratum, hospital-heterogeneity and persons-vs-episodes defences); `fig3_rem_pathway` (definition eras in separate facets, establishments beside every count, stocks and flows on separate axes: the rules a reviewer must see, not read); `fig4_triangulation` (education, surveys with n and cases, indices declared as indices, per-population strips, DEIS vs GRD principal). Table 1 = a 24-row subset of `T2_grd_core` (rows listed in the plan); Table 2 = a 30-model subset of `T7_models` selected by model identifier. Reason: a Lancet Article carries few display items; the six chosen are the ones whose absence would make the paper unreviewable (design, primary estimand, sensitivity models, convergence). `fig1_sources_coverage` (today's Figure 2) moves to the supplement as Figure S1: it is data-quality context (RECORD 12.1/12.3), not a result.
2. **Journal-format changes to the plates are made behind a flag** (`common.plate_resolve`/`save_fig` journal mode, default off): no in-panel bold titles (lowercase letter only; the descriptions already open each legend sentence); no inset title; legend frames off; no box; decimals with the mid-height point in artwork labels; the `without Rett` corner tag of Figure 1 goes into the legend. Composition must be re-verified with `PLATE_CHECK=strict` because removing titles changes the headroom the checker measured.
3. **Supplement opens with the applied methodology** (M1–M7, all 33 equations each once and in order, the 26-estimator map, the 10 methods tables as Supplementary Tables S1–S10), then the STROBE and RECORD checklist in English, then the analysis plan (English rendering of v1.0 with the original Spanish text supplied verbatim as a separate file), then the results in **order of first citation from the article** (methods tables first), 35 figures and 62 result tables. Reason: the journal requires one PDF with a table of contents and the author requires the methodology and every important result that left the body.
4. **Dropped from the supplement** (reasons in the plan): duplicates of a kept item (figS1 vs figE28; figS8 vs E14/ST13; S8 vs ST13; S5 vs ST14; F3 vs T3; E15/E15b; E18 vs figS13; E1–E7 vs the EF companions; S15 vs T8), lower-priority hospital detail (EF1, EF2, EF4, EF7, EF9), place-of-care regional flows for 2023–2025 only (E13, E16), context plates the text does not use (E20, E21, E22, E25), spatial items that invite a cross-source or causal reading (E45 bivariate Moran, E48, E49, E50, E43 figure), and the complete data tables E60–E81, ST7b, which go to the public repository named in the data sharing statement.
5. **References cut from 50 to 30** by keeping the ten primary dataset citations, the seven core methods citations, the law, and twelve context/discussion citations; the five sensitivity-layer dataset citations (`ine_base2024`, `ine_censo2024`, `fonasa_aps`, `supersalud_isapre`, `deis_rem20`) and the SAGER paper move to the supplement's own reference list, where the extended methodology cites them. Every dropped key carries a reason in the plan.
6. **Standing-rule guards written into the plan**: the DEIS/GRD principal ratio (1·74) is removed from the body (a cross-source ratio); the "gap between growth per establishment and per resident quantifies reporting expansion" claim is softened to a bound; the Summary and every legend say administrative recognition, never prevalence; Law 21.545 is context in every plate; place-of-care numerators over residence denominators keep their warning in Figure 2d and Figure 4e; indices are declared as indices; no cascade wording.
7. **Word budget** 4,500 (Introduction 450, Methods 1,450, Results 1,500, Discussion with conclusion 1,100), ceiling 4,900; Summary 243 words drafted with its value keys (ceiling 250).
8. **Not decided here, flagged as risks**: superscript citations after punctuation and mid-height decimals need new renderers with a protected-token list (ICD codes, REM codes, `Law 21.545`, versions, DOIs, URLs); 10-pt single-spaced legends will not fit under a 180 × 245 mm plate, so legends must be allowed to continue on the following page; supplementary cross-references stored in `captions.json`/`titles.json` use the corpus numbering and must be remapped; second author, affiliation, funding, ethics, repository URL/DOI and AI-tool versions are author-supplied and must never be invented.

Nothing in the existing corpus was modified by this task; only this file was created/appended.
## 2026-09-09 — Task A (lens `A_journal_editor`): editorial design of the sin_rett journal submission

Decisions taken while designing the focused paper; nothing in the corpus was modified. Sources read: `journal_guidelines.md` and the archived PDF text, `manuscript/article_sin_rett_en.pdf` (65 pp), `prose_en.py`, `outputs/values_sin_rett.json` (17,198 keys), `supplementary_material.py` (FIGURE_ORDER 54, TABLE_ORDER 125), the four `captions.json`/`titles.json`, `prose_methods_extended.py` (`methods_blocks`: 59 paragraphs, 26 estimator headings, 33 equations, 10 tables, 6,769 words), `equations.py` (33 equations, 26 estimators, no orphans), `references.bib`, `decision_log.md` (standing rules, open decisions), `memo_es.md` §5–6, `figure_table_plan.md`, `analysis_plan.md`, `reporting_checklist.md`, and the plates `fig2_grd_core`, `fig1_dataflow`, `fig3_rem_pathway`, `fig4_triangulation`, `figS4_models_cpa` read at reduced size.

| # | Decision | Reason |
|---|---|---|
| A1 | Body display items = six: Figures 1–4 (`fig1_dataflow`, `fig2_grd_core`, `fig3_rem_pathway`, `fig4_triangulation`) and Tables 1–2 (`T2_grd_core`, `T7_models`, both as journal row cuts whose full versions are appendix Tables S38 and S77). The working article's Figure 2 (`fig1_sources_coverage`) and Tables 2–5 and 7 (`T3_rem_pathway`, `T4_denominators_coverage`, `T5_survey_benchmarks`, `T6_education`, `T8_controls_compact`) move to the appendix. | Lancet Articles carry few display items; the four figures are the RECORD flow, the primary estimand, the outpatient pathway and the convergence/benchmark message; the two tables are the estimand with its sensitivities and the pre-specified models. Everything else is documentation of the data or detail the text summarises in one sentence. |
| A2 | Journal plate mode: strip the descriptive in-panel titles and keep only the lowercase letter `(a)`–`(f)` at the panel; the figure heading (from `captions.json` title) is printed at the start of the legend in 10 pt Times New Roman bold, legend 10 pt single-spaced; the variant tag ("without Rett") and the "— F84 without Rett variant" title suffix move from the plate to the legend; no outer box (already none); 180 mm × 245 mm at 600 dpi kept (journal minimum 107 mm, 300 dpi); one figure per page. Implemented behind an environment flag in the shared plate engine (default = today's behaviour) and re-checked with `PLATE_CHECK=strict`. | Journal: "Do not use titles in the graph or artwork. Titles should appear at the beginning of the figure legend"; lowercase panel letters; no box. Verified visually that every panel of Figures 2–4 currently carries a bold in-graph title. |
| A3 | Supplement = ONE English PDF, TOC with page numbers, numbered pages, 10 pt TNR, 12 pt bold main heading, 10 pt bold headings. Part A opens with the applied methodology `methods_blocks('sin_rett', V, 'en')` verbatim (M1–M7, all 33 equations once and in order, the estimator map as Table S5, methodological Tables S1–S10), then the STROBE/RECORD checklist (Table S11) and the pre-specified analysis plan rendered in English as the protocol. Part B carries the results that did not make the body, numbered by order of first citation from the article: Figures S1–S37 and Tables S12–S89. The working version's short "Supplementary methods S1–S5" and "Optional material index" are not printed (superseded by Part A). | Author requirement: the supplement MUST carry the applied methodology; journal: appendix items numbered by order of mention, references numbered separately. |
| A4 | Supplement selection: 37 of 55 candidate figures and 81 of 125 supplementary tables kept, plus the seven relocated main tables and the checklist. Dropped (with reason, one by one in the plan): the hospital-episode descriptive plates EF1–EF4, EF7, EF9 and their tables (length of stay, severity, seasonality, single-year age, territory-by-residence duplicates of the spatial set); REM regional/age/monthly duplicates E13, E14, E16, E18; population/insurance/JUNAEB duplicates E20, E21, E25; spatial duplicates E41, E43 plate, E44 plate, E45, E48 plate; the 18 "complete data" tables that only re-list what the kept tables and the shared tidy files carry (E61–E68, E70–E78, E81) except E60, E69 and E80. | "Very precise": every kept item answers a reviewer question the body raises; every dropped item is either descriptive without bearing on the message or duplicated by a kept item, and stays in the repository named by the data-sharing statement. |
| A5 | References cut 50 → 30: the 15 official dataset sources, the law, five methods references (STROBE, RECORD, SAGER, quasi-Poisson, WHO standard) and nine context references (`zeidan2022`, `russell2022`, `hansen2015`, `shaw2025`, `lord2022`, `montielnava2024`, `yanez2021`, `romanurrestarazu2025`, `loomes2017`). `fay1997`, `wolter2007` and the other 30 method references remain in the appendix's own list, which the journal numbers separately. `becerril2011` is dropped because the segmentation sentence can rest on the study's own coverage data (Table S25, `share_fonasa_ine_pct_2025`). | Journal: maximum 30 references; the official sources of every dataset must be in the article (RECORD 1.2, 6.1); the 15 sources leave nine slots for context. |
| A6 | The "Official sources" paragraph (104 words, currently `[OPT]`) returns to the core Methods; the other ten optional paragraphs are either dropped or compressed to a sentence (education definitions, sensitivities list, hospital heterogeneity, DEIS, coverage layers, REM APCs, strengths). A short Results paragraph on territory (≈80 words) is added, reading Moran's I, the LISA class counts and the Gini from the spatial tidy tables (no values key exists for them). | The dataset citations cannot be optional; the territorial analysis is computed and is otherwise absent from the body. |
| A7 | Word budget: Introduction 400, Methods 1,300, Results 1,650, Discussion (incl. Conclusion) 1,000 → 4,350, hard ceiling 4,700, measured with `prose_en.word_counts` on the journal blocks (manuscript text only: Summary, panel, legends, tables and declarations excluded). Summary ≤ 250 words, five paragraphs, with the sex-ratio sentence added (SAGER). | 3,500–5,000 with headroom. |
| A8 | Numbers: every value in the plan is a key of `values_sin_rett.json` or a row of `outputs/tidy/spatial_moran.csv` / `spatial_inequality.csv` / `spatial_lisa.csv` (sin_rett rows), named in the plan; mid-height decimals, numbers one to ten in words, p to two significant figures (`p<0·0001` below) applied in the print funnel, never to identifiers, codes or version strings. | Standing rule: nothing typed by hand. |
| A9 | Risks recorded in the plan: single author vs "more than one author verified the data"; data-sharing URL/DOI must exist at submission ("undecided" not accepted); ethics confirmation; PDF conversion resamples plates to 300 dpi (submit DOCX or force 600 dpi export); the con_rett variant is not submitted and lives in appendix Figure S4/Table S22; REM 2025 possibly incomplete; the journal-mode plate change must pass `check_layout`; first-citation numbering needs a test. | — |

Baseline measured by this task before any journal work: `python -m pytest tests -q` → 299 passed, 1 skipped, 157 subtests passed (432 s, 2026-09-09).

## Judge 1 — editorial panel verdict on plans A/B/C (2026-09-09)

Winner: plan A (`A_journal_editor`), scores A 40, B 39, C 36 of 50. Plans B and C reached the judge only through their entries above (the orchestrator's message was truncated after plan A); they were scored from those entries. Facts verified against the corpus, binding on the builder:

- Every figure/table key plan A names exists in `supplementary_material.FIGURE_ORDER`/`TABLE_ORDER` and in `outputs/sin_rett/en`; two slips: the E41 table is `E41_rem_comuna_place_of_care`, the E45 table `E45_bivariate_moran_pairs`. Every reference key named by the three plans exists in `references.bib`.
- Values keys: the convergence indices are `conv_grd_any_rate_index2021_2024`, `conv_deis_principal_rate_index2021_2024`, `conv_a05_strict_entries_index2021_2025`, `conv_p2_december_stock_index2021_2025`, `conv_pie_harmonised_index2021_2025`; depth-stratum ratios `grd_depth_bin_{3,4,5,6_7,8_10,11plus}_rate_ratio_2024_2019` exist (2·48–4·95), bins 1–2 have no ratio key. No key was found for "2·39% physician-confirmed", "10·1% of PIE registrations" (only `pie_harmonised_share_of_pie_pct_2022` = 9·787), "76·8-fold", regional ρ 0·38, REM-20 ρ 0·70, "1·3% explicit zeros"; the Census effect must be derived from `ine_ratio_censo2024_base2017_2024` (0·920) in the print funnel.
- Plan A's Summary draft is 267 words (45/61/107/53/1); the article's current Summary is 249, plan B's 243.
- `tests/test_supplementary_registry.py::test_no_plate_file_is_left_unregistered` scans `figures/` and `extra/figures/` (`_plate_stems`); journal-mode plates must be written elsewhere (plan C's `outputs/<variant>/<lang>/journal/figures/`).
- `decision_log.md` (2026-09-04, 01b verificación) accepts the DEIS/GRD principal-vs-principal ratio (`deis_vs_grd_ratio_f84_principal_2024` = 1·74) as a coverage comparison with a non-probability note; the any-position/principal ratio is never used.
- Spatial numbers for the added territory paragraph are in `outputs/tidy/spatial_moran.csv` (sin_rett, comuna, grd_episodes, sir_eb, queen, full period: I = 0·340, p_sim 0·001), `spatial_lisa.csv` (BH-rejected classes HH 12, LL 6, LH 1; Gi* hot 13, cold 6) and `spatial_inequality.csv` (Gini 0·373 in 2019, 0·299 in 2024).
- "Trough 2020" holds for GRD counts (2334→1609) and DEIS (304→255), not for the GRD rate (202·7→205·8), P2 December (1854→1919) or PIE (21 012→23 590).
- T2_grd_core has 45 rows, T3_rem_pathway 54, T7_models 54, T7_models_cpa_full 220.

Nothing in the corpus was modified by this task; only this file was appended.

## Task: SYNTHESISE THE FINAL PLAN — `journal/plan.md` (2026-09-09)

Provenance. `plan.md` is the executable synthesis of plans A (`A_journal_editor`, the judges' winner), B (`B_methods_reviewer`) and C (`C_policy_reader`) and of the two judgements (both scored A 40, B 39, C 36–37). It was written after re-reading the corpus rather than the plans' descriptions of it: `prose_en.py` (the value keys of every paragraph extracted by AST from `_assemble`, so the plan names the keys the corpus really uses — e.g. `grd_hosp2024_rate_ratio_max_min`, `svy_endide_children_reported_confirmed_total_pct`, `pie_tea_strict_share_of_pie_pct_2023`, `apc_grd_hospital_ri_re_sd` — instead of the guessed names the judges could not find), `supplementary_material.py`, `prose_methods_extended.methods_blocks` (136 blocks, 39 bib keys), `equations`, `docx_builder.build_document` (block grammar and caption-spill machinery), `pipeline/10_manuscript.py`, `common.py` (plate funnels and `PLATE_CHECK`), `paper/references.py` (parenthesised citations, six-author limit), `values_sin_rett.json`, `T2_grd_core.csv` (45 rows), `T7_models.csv` + `T7_models_numeric.csv` (row-aligned, `model_id` and raw `p_value`), `outputs/tidy/spatial_*.csv`, the four `captions.json`/`titles.json`, `references.bib`, `journal_guidelines.md`, `analysis_plan.md`, `reporting_checklist.md`, `tests/` (300 collected). This file already carried the entries of tasks A, B, C and Judge 1; they were kept and this entry appended (the instruction to create an "empty" decisions.md was read as "append your provenance", never as deleting other tasks' records).

Decisions (what was taken from the winner, what was grafted, what was fixed, and why):

1. **Body = A's six items** (Figures 1–4 = `fig1_dataflow`, `fig2_grd_core`, `fig3_rem_pathway`, `fig4_triangulation`; Table 1 = `T2_grd_core` cut; Table 2 = `T7_models` cut). C's Table 2 (a `T3_rem_pathway` cut) was rejected because the paper's question is robustness and the pre-specified models must be in the body; T3 is appendix Table S49.
2. **Row selection as a presentation filter (C) addressed by labels (B)**: Table 1 by exact (`Block`, `Indicator`) pairs — 25 rows, verified against the CSV; the two "mean (median)" coding-depth rows are excluded because the journal wants mean (SD)/median (IQR) and the cells cannot be recomputed. Table 2 by `model_id` through the row-aligned `T7_models_numeric.csv` (B's "by model identifier" was not directly feasible on `T7_models.csv`, which has no id column) — 29 rows, all verified, in file order; the p column is recomputed from the raw `p_value` (two significant figures, "<0·0001"), never from the "< 0.001" string.
3. **Journal plate mode (C's route)**: `PLATE_JOURNAL` default off, applied in `common.plate_resolve` (titles reduced to the bare lowercase letter, variant stamp removed, artwork "100,000"/decimals converted) and in a `journal_redirect` used by `save_fig`, `merge_json`, `atomic_write_json` and `plate_check_write`, so that journal PNGs, captions and runlogs land in `outputs/sin_rett/<lang>/journal/figures/` and `outputs/controls/journal/` and the corpus folders (scanned by `test_no_plate_file_is_left_unregistered`) are never written; re-rendered with `PLATE_CHECK=strict`; only the four body plates use journal mode, supplementary plates are read from the corpus.
4. **Summary rewritten and measured**: 248 words with `prose_en.word_counts` (40/56/102/48/2), keeping the SAGER sex-ratio sentence and naming ENDIDE 2022 and ENCAVI 2023–24 separately (the judges' disability-rule fix); "mean coding depth 4·39 → 5·78" left the Summary (it stays in Results and Figure 2c); counts are measured before the number pass because the narrow-space thousands separator would inflate `str.split()` counts.
5. **Research in context** reused from the corpus (418 words, no citations, exact search date) with one added word ("rehabilitation") in Added value.
6. **Key-message and prose fixes required by the judges**: "trough 2020" restricted to hospital counts (rate and stocks did not fall — keys `grd_f84_any_rate_2020`, `p2_tea_dec_2020`, `pie_harmonised_2020`); "per establishment vs per resident" wording changed from "quantifies" to "bounds"; "rose within every coding-depth band" restricted to strata with a ratio key (three or more diagnoses), strata one and two printed as rates only; the DEIS/GRD principal ratio kept in the body only with the corpus's "coverage comparison, not a probability" wording (decision_log 2026-09-04, 01b verificación), the any/principal ratio never printed; the international-comparison sentence no longer implies that flows are a prevalence.
7. **Territory paragraph added to Results (A6)** with every number located in a tidy row by file, filter and column (Moran's I 0·340 queen/sir_eb/full period; LISA 12 HH, 6 LL, 1 LH; Gi* 13 hot, 6 cold; Gini 0·373 → 0·299; comunas with events 242 → 302; top-decile share 25·7% → 20·6%; GRD–A05 comuna ρ 0·27 with its "two unlinked systems" warning), exposed through `journal_config.tidy_value` so the prose types nothing.
8. **Supplement = Part A (applied methodology) then Part B by first citation**: A1–A7 = `methods_blocks` verbatim with the 33 equations once each in numeric order and the estimator map; A8 = STROBE/RECORD checklist as Table S11 (English, "where" remapped); A9 = analysis plan v1.0 (4 Sept 2026) in English, the Spanish original copied verbatim to `manuscript/journal/analysis_plan_v1.0_es.md` (B's graft); A10 = separately numbered appendix references. Part B = 36 figures and 74 result tables (85 tables in all), numbered by the order in which the journal article first cites them, with the first-citing paragraph named for every item; B's duplicate rule applied (figS1 vs figE28, S8 vs ST13, S5 vs ST14, F3 vs T3+E69, T8 full/S15 vs T8_compact+E79+figS15); C's front-matter statement that the con_rett variant is not submitted and lives in Figure S2/Table S24; A's territorial and co-diagnosis items kept, E45 (bivariate Moran) dropped with B as inviting a cross-source reading. Reconciled against the registry: 35 registry figures kept + `fig1_sources_coverage` = 36; 19 figures dropped; 77 registry tables kept + 7 relocated body tables + 1 synthetic checklist = 85; 48 tables dropped; every kept key exists in `outputs/sin_rett/en/{figures,extra/figures,tables,extra/tables}`.
9. **References = 30** in first-citation order: 10 primary dataset citations + the law + 5 methods (STROBE, RECORD, SAGER kept in the article as the judges asked, WHO standard, Fay–Feuer for Table 1's standardised limits, quasi-Poisson) + 13 context (zeidan2022, russell2022, hansen2015, lundstrom2015, shaw2025, lord2022, montielnava2024, yanez2021, romanurrestarazu2025, iezzoni1992, song2010, loomes2017, garcia2022). The five sensitivity-layer datasets and wolter2007 move to the appendix list (B's graft); irarrazaval2023 and cid2024 were not taken (ley21545 suffices; the payment clause is deleted) so that iezzoni1992, song2010, lundstrom2015 and garcia2022 fit. All 30 keys and the 20 dropped keys verified in `references.bib`.
10. **API contract fixed for four parallel builders** (plan section 7): `journal_config` (VARIANT, BODY_*, TABLE_ROWS, TABLE_COLUMNS, SUPP_FIGURES, SUPP_TABLES, REFERENCE_KEYS, WORD_BUDGET, PROTECTED_TOKENS, `journal_text`, `format_p`, `tidy_value`, `asset_path`, `JournalRegistry` with `assert_monotone`), `prose_journal_en.article(V)`, `prose_journal_es.article(V)`, `supplement_journal.blocks(lang, V)`, `build_journal.py --lang/--skip-pdf/--render-plates`; outputs under `manuscript/journal/` (invisible to the tests, which glob `manuscript/` non-recursively); `docx_builder.build_document(..., journal=None)` flag for 10/8/8 pt tables, 10 pt legends with the bold heading, superscript citations after punctuation (the shared `Citations` prints "(1,2)" today), one figure per page, supplement TOC by a two-pass build with page verification; the journal author rule (7+ authors → first three, et al) and en-rule page ranges patched onto `format_vancouver` in the journal build only.
11. **Verification** (plan section 8): eight reader criteria on the rendered PDFs and PNGs, including the corpus-integrity check (SHA-256 of `outputs/sin_rett`, `values_*.json`, `tidy`, `manuscript/*` unchanged; pytest baseline 299 passed, 1 skipped) and the forbidden-phrase list for the standing rules.

Nothing in the corpus was modified by this task; `journal/plan.md` was created and this file appended.

## Task B3 — `journal/supplement_journal.py`, the ONE English supplementary appendix (2026-09-09)

Read before building: `journal/plan.md` (sections 4, 5, 7.2, 7.6, 7.7, 8 R5–R6), `supplementary_material.py` (FIGURE_ORDER/TABLE_ORDER, FIGURE_INTRO, W, material_blocks), `prose_methods_extended.methods_blocks` (136 blocks: 59 paragraphs, 26 estimator headings, 33 equations, 10 tables, 39 bib keys; `heading_equation_problems`, `table_order_problems`), `equations` (NUMBER 33, ESTIMATORS 26, LABEL), `prose_en._Registry` and its helpers (`_strip_prefix`, `table_note`, `panel_suffix`, the formatters n0/n1/n2/pct/ppct/nw/lst), `docx_builder.build_document` (block grammar: title, subtitle, authors, pagebreak, h1–h3, p, small, bullets, eq, table, figure, panel, refs), `pipeline/10_manuscript.py` (title_page, build_pair, to_pdf, postprocess_docx), `pipeline/17_extended_material.py`, `reporting_checklist.md`, `analysis_plan.md`, `journal_guidelines.md`, `outputs/values_sin_rett.json` (every key the module prints was looked up), the four `captions.json`/`titles.json` of `outputs/sin_rett/en`, `tests/test_17_extended_material.py`, `tests/test_supplementary_registry.py`, `tests/test_fixed_panel_gloss.py` (which scans every `.py` of the package for the canonical fixed-panel clause: the clause is not copied here), and, once they landed during the task, `journal/journal_config.py` (JournalRegistry, `refs_remapped`, `remap_refs`, `journal_text_blocks`, SYNTHETIC_TABLES) and `journal/build_journal.py` (its own `toc_entries`/`with_toc`/`locate_entries`, inserted before the first h1) and `journal/prose_journal_en.py` (the cross-reference partner).

Decisions:

1. **Registry and order.** `SUPP_FIGURES` (36) and `SUPP_TABLES` (85) are the plan's section-4 lists in first-citation order, each key paired with the article paragraph that first cites it (`FIRST_CITED`, 23 ordered sections); `_check_registry_order()` fails the import if an S-number would not ascend with that order. Tables S1–S10 are `prose_methods_extended.TABLE_KEYS` (printed inside Part A by `methods_blocks`) and S11 the synthetic checklist `S_reporting_checklist`. The lists are verified equal to `journal_config.SUPP_FIGURES/SUPP_TABLES` by a test; every key was checked to exist on disk under `outputs/sin_rett/en/{figures,extra/figures,tables,extra/tables}`.
2. **Part A verbatim.** A1–A7 are `methods_blocks('sin_rett', V, 'en', R=registry, first_table=1)` untouched except the h1, which becomes "Part A. Applied methodology" preceded by one orientation paragraph. The 33 equations therefore print once each, in numeric order, under the estimator heading that cites them (checked by `equation_problems`, which re-runs `PME.heading_equation_problems` on the assembled document); the estimator map and the sensitivity grid print as Tables S5 and S6 where they are discussed and are named in the orientation paragraph by their registry labels. A8 = the STROBE (22) + RECORD (13) checklist written in English from `reporting_checklist.md` with "Where reported" mapped to the journal sections and to the journal S-numbers (labels computed from the registry lists, never typed); statuses from a closed set, "Reported in structure; content author-supplied" for STROBE 22 and RECORD 22.1, "Not applicable" for RECORD 12.2. A9 = an English rendering of `analysis_plan.md` v1.0 (4 September 2026), heading by heading; the module asserts the Spanish stamp is still in the file before rendering. A10 = `("refs", {"title": "Appendix references"})`, a separately numbered list of every key the appendix cites (the 39 keys of `methods_blocks`, which include the five relocated dataset citations, `wolter2007`, `who_icd10`, `deis_establecimientos` and the statistical references; verified against `references.bib`).
3. **Part B, one paragraph per item.** Every figure is introduced by `supplementary_material.figure_intro` (what it shows, unit, denominator; reused verbatim) followed by a sentence written here that says why it matters and what it adds beyond the body (`FIGURE_ADDS`; `fig1_sources_coverage`, a former body figure without a corpus sentence, gets its own "shows" text in `FIGURE_SHOWS_EXTRA`). Every table is introduced by one sentence (`TABLE_INTROS`). Items are grouped under eleven B-headings that follow the article's order; inside a group, figures precede tables so that both S-sequences ascend on the page. Duplications between the corpus "shows" sentence and the added sentence were removed after proofreading the assembled text.
4. **Numbers.** Every number these paragraphs print comes from `values_sin_rett.json` through the `prose_en` formatters with the key named in `_fields` (e.g. `grd_f84_secondary_only_share_2024_pct`, `grd_episodes_per_person_any_2024`, `grd_hosp_rr_fixed_effects_none_n_above1/_below1`, `apc_grd_hospital_ri_re_sd`, `grd_records_fixed65_share_2024`, `a05_autism_entries_stable_total_2021/_2025`, `a05_autism_pop_total_asr_2021/_2025`, `p2_tea_share_of_naneas_dec_pct_2023/_2025`, `p2_jun_dec_ratio_2020`, `pie_tea_strict_share_of_pie_pct_2023`, `deis_vs_grd_ratio_f84_principal_2024`, `ine_ratio_censo2024_base2017_2024`, `coverage_2025_share_fonasa_ine_age_0_9`, `apc_grd_any_obs_disp`, `apc_grd_any_sensitivity_min/_max/_n_specs`, `grd_f84_any_mf_ratio_n_2019/_2024`, `a05_autism_pop_asr_mf_ratio_2025`, `t8_rows/ok/differs`, `prov_artefacts_n`, `rem_pathway_codes_n`, `grd_fixed_panel_n`, `grd_hospitals_ever_observed_n`, `grd_hospitals_observed_{2019..2024}`, `models_n_variant`), from the row count of the table being printed (`R.nrows`) or from the size of a code registry (`EQ.ESTIMATORS`, `EQ.NUMBER`, `PME.SENSITIVITY_GRID`). The JUNAEB male:female range is the min and max of the four named 2025 keys (the same derivation the plan uses for the Discussion). Caption-internal numbers (1·3 %, 0·38, 0·70) are never re-typed. Numbers are written in the corpus English convention; the journal number pass of `build_journal` converts them after counting.
5. **Cross-references, remapped exactly once.** Captions and notes of the corpus carry corpus numbering. `journal_config.JournalRegistry` remaps them and flags the payload `refs_remapped`; `build_journal.journal_text_blocks` remaps only unflagged payloads. This module therefore leaves payloads untouched by default (`remap=False`) and, for standalone verification (`remap=True`), remaps through `journal_config.remap_refs` (or its local fallback `_local_remap`) while honouring the flag, so no token is ever remapped twice (the double-remap "Figure 5 → Figure 4 → Figure 3" was observed during development and is what the flag prevents). The prose written here carries journal labels from the registry lists and is never remapped.
6. **Table of contents.** `build_journal` inserts its own `("toc", …)` block before the first h1 and fills the pages in its two-pass build, so `blocks()` emits no TOC by default; the front matter (title, subtitle, authors, the about-paragraph with the con_rett statement, the standing rules in prose) carries no heading so that the builder's TOC lands after it, as the plan orders the front matter. For standalone builds `blocks(..., toc=True, pages=…)` prints the module's own TOC (one 10 pt line per Part, M-section, A8/A9, B-group, figure, table and the appendix references; `toc_entries()` gives the ids). Figure payloads receive a `heading` field (the legend heading) that the builder's TOC uses.
7. **No registry side effects.** Labels are positions in the registry lists (`fig_label`, `tab_label`, `mfig_label`, `mtab_label`), so a registry shared with the article is never marked as having cited every appendix item; `R` is used only for blocks, paths and row counts, and by `methods_blocks` for its ten tables. `checklist_table(R=None)` keeps the contract's surface.
8. **Standing rules in the module's own text.** The R6 phrases (prevalence/incidence of autism as a claim, "hospitalisations for autism", "cascade", "conversion", "effect of Law", "caused by", "attributable to the law", "trough in 2020", "quantifies the contribution of reporting") do not occur in any string this module writes (test); the analysis-plan rules are rendered without them ("never described as admissions for autism"; "no change can be identified with Law 21.545"). The reused corpus note sentence that contained "care cascade" was rewritten ("no figure or table follows individuals across systems"). The DEIS/GRD comparison is always "a coverage comparison between two unlinked registries of the same event, never a probability"; place-of-care over residence carries its warning; December primary / June sensitivity, never summed.
9. **Tests** (`tests/test_supplement_journal.py`, 21 tests): registry sizes, uniqueness and first-citation order; traceability of every key; presentation text for every item; agreement with `journal_config`; "es" refused; forbidden phrases absent; checklist shape, statuses and S-numbers within range; analysis-plan rendering and source stamp; local remap and the citation parser (ranges "Tables S27–S29" and lists "Tables S45 and S46" expanded); on the assembled appendix: 33 equations once each in order and cited, Tables S1–S85 and Figures S1–S36 in order each preceded by its paragraph, Part A before Part B, references last, no corpus numbering surviving, every asset on disk, TOC covering every item, the con_rett statement; and BOTH directions of the cross-reference with `prose_journal_en.article(V)` (every S-item the article cites exists; every appendix item is cited; first citations ascend) — this last test skips with a reason while `prose_journal_en` is not importable. Against builder A's current article: 0 problems, figures cited 1…36 and tables 1…85 in ascending order.
10. **Verified on the rendered page**, not on a report: a standalone build (docx_builder + LibreOffice, plates embedded at 150 dpi for speed, in the scratchpad, never in `manuscript/`) gave 447 A4 pages, 39 appendix references, 85 tables, 36 figures; read on the PDF: title page and about/rules on pp 1–2, contents pp 3–9, Part A from p 10 (M1 p 11, M4 p 22 with the equation images labelled "Eq. n" under headings that cite them), A8 with Table S11 p 47, A9 p 51, Part B from p 55 (Figure S1 p 68 as a full-page plate with its legend, Figure S36 p 424, Table S85 p 429), appendix references p 444. Pages 4, 23 and 68 were also inspected as images. Journal typography (10 pt body, 10/8/8 pt tables, 10 pt legends, superscripts) is builder P/B's `journal` flag of `docx_builder` and was not exercised here.

Files touched: `journal/supplement_journal.py` (new), `tests/test_supplement_journal.py` (new), this file. Nothing in the corpus was modified.

## Task B4 — journal-format builder: `journal/build_journal.py`, journal flags of `docx_builder.py` and `pipeline/10_manuscript.py` (2026-09-09)

Read before building: `journal/plan.md` (section 7 contract, sections 2–4 for the display items and the supplement order, 8–9 for the verification criteria and risks); `docx_builder.py` in full (block grammar, `plan_full_page_plates`/`plate_layout`/`split_caption_lines`, the number-width guard `numeros_de`/`_aplicar_anchos`, `insertar_tabla`, `pie_figura`, `panel`, `_add_text`); `pipeline/10_manuscript.py` (`build_pair`, `title_page`, `localised_references`, `postprocess_docx`, `landscape_wide_tables`, `add_page_numbers`, `to_pdf`, `page_layout_audit`, `thumbnails`); `paper/references.py` (`Citations`, `_format_numbers`, `_author_list`, `format_vancouver`); `prose_en.py` (`_Registry`, `article`, `word_counts`, `citation_keys`); `supplementary_material.py`; `prose_methods_extended.methods_blocks`; `journal_guidelines.md` and the archived PDF text (references, figures, tables, supplementary material); `tests/test_10_manuscript.py`, `test_table_numbers_unbroken.py`, `test_percent_and_code_breaks.py` (what the guards expect of `numeros_de`).

Decisions:

1. **Default behaviour of the shared builders is byte-identical.** Every change to `docx_builder.py` and `10_manuscript.py` sits behind an argument whose default keeps today's code path: `build_document(..., journal=None)`, `plan_full_page_plates(..., journal=None)`, `plate_layout(..., lead_ladder=None)`, `insertar_tabla(..., block_bold=False, max_pt=None)`, `rotulo_tabla(..., pt=11, bold_title=False)`, `nota_tabla(..., size=9)`, `pie_figura(..., heading=None)`, `insertar_ecuacion(..., size=12)`, `panel(..., pt=None)`, `titulo(..., pt=None)`, `localised_references(lang, journal=False)`, `landscape_wide_tables(doc, lang, force_labels=())`, `postprocess_docx(path, lang, landscape_labels=())`. Verified, not assumed: two documents built in default mode before and after the patch (the corpus article with its five plates; a supplement slice with six lead-in plates and four tables, post-processed) have every zip member byte-identical (36 and 80 members). Two shared regexes gained one character — the mid-height decimal «·» inside the numeric-atom class of `_NUM_ATOM_RE`/`_NUM_ETIQUETA_RE` (and its width in `_ANCHO_NUM`) — so that the journal tables' numbers («804·1») are protected from breaking like the corpus's («804.1»); no corpus atom carries «·», so the guard's decisions there are unchanged (the two guard tests pass).
2. **Superscript citations after punctuation** are rendered by `docx_builder.journal_resolve`: the marker `[@a; @b]` becomes the number(s) wrapped in private-use characters placed AFTER the punctuation that immediately follows the marker (`\s*\[(@…)\]([,.;:]?)`), printed by `_add_plain` as a superscript run; two references «1,2», three or more consecutive «1–3» with an en rule (the shared `Citations._format_numbers` output with its parentheses stripped and the hyphen replaced). Because the journal article prints the tables and figures AFTER the References, `prenumber_citations` numbers every key in block order (paragraphs, panel, bullets, table notes, figure captions) before anything is written — the same order as `prose_en.citation_keys` — so a key cited only in a display item is still in the list.
3. **Journal reference rule** in `10_manuscript.localised_references(lang, journal=True)`: `_author_list` is patched so that a list with more than six authors — or a `.bib` list truncated with «and others», which means the same — prints the first three and «et al» (six or fewer: all); the `pages` field takes an en rule before formatting (`journal_reference_entry`), never the DOI, whose hyphens are not ranges. Verified on zeidan2022 (3 + et al), shaw2025/lord2022 (six named + others → 3 + et al), vonelm2007 (exactly six → all six), pages «778–790».
4. **Tables**: label and title as two bold 10 pt paragraphs (two paragraphs, not one, because `landscape_wide_tables` recognises the label paragraph two above the table and carries it into the landscape section); body 8 pt (`max_pt=8` caps the automatic size, and a table that does not fit even landscape still steps down to 6 pt as today rather than breaking words); the note at 8 pt; internal headings 8 pt bold ACROSS the row: a payload `heading_column` (the contract's Block/Estimand column) is turned into heading rows by `agrupar_por_columna` (one per consecutive run) and each heading row is merged into one cell (`_aplicar_anchos` became span-aware — a merged cell measures the sum of its columns — and the empty paragraphs the merge concatenates are removed, which otherwise printed seven blank lines under each heading). The body tables are printed landscape even when their longest words would fit 16 cm (`landscape_labels`), as the plan states for Tables 1–2; the supplement keeps the width rule.
5. **Figures one per page with 10 pt legends**: in journal mode `plan_full_page_plates` fixes the legend ladder at `legend_pt` (10) and the lead-in at `body_pt`, so the legend is never shrunk (the corpus ladder goes down to 7 pt) — what does not fit under the 245 mm plate is split by the existing machinery and continues on the next page labelled «(continued)»; and with `one_figure_per_page` every plate opens its own page with the whole page budget, so a legend tail never shares a page with the next plate (the alternative — the corpus chain that lets the tail sit above the next plate — would cascade shorter and shorter legend heads at 10 pt). The figure heading (`heading` in the payload, or the first sentence of the caption when absent) is printed bold as the first run of the legend after the label; the plate stays 180 mm at 600 dpi (embedded without resampling in the article, at `--supp-dpi` 300 in the supplement).
6. **Number pass and its check**: `build_journal` calls `journal_config.journal_text_blocks` when it exists (it does) and otherwise a fallback with the contract's rules (protected tokens masked; «2,334» → «2334»; five or more digits with U+202F; «.» → «·» between digits); in both cases `numbers_unchanged` compares, string by string and block by block, the normalised numeric tokens before and after (separators removed, «·» read as «.»), ignoring figure/table cross-reference tokens (which the remap of contract 7.7 renumbers) and raises if any value changed; `ascii_decimals_left` reports any ASCII decimal surviving outside the protected tokens (none in the current build: 592 strings checked in the article).
7. **Supplement TOC** (`("toc", …)` block, journal-only): one line per h1/h2 heading, table, figure and the appendix references, with a right tab and dot leader; pass 1 writes a three-digit placeholder, converts to PDF and locates every entry with `pdftotext -layout` (tables by the label alone on its line, figures by «Figure Sn.» at a line start, headings by their first 50 characters, searching monotonically from the page after the TOC — identified as the first page whose first line is the Part A heading); pass 2 writes the pages and verifies every entry again (a third pass is allowed; a mismatch or an entry not found fails the build). The TOC replaces the supplement module's own «Contents» heading and placeholder lines when present, else it goes before the first «Part A» h1. With `--skip-pdf` the TOC is written with «—» and flagged unverified in the report.
8. **Title page**: when the prose/supplement module supplies its own `authors` payload with `lines` (the contract lets it word the title page), those lines are kept and the build stamp appended; otherwise the lines are composed from the measured counts (never typed). The subtitle block is kept when supplied.
9. **Typography chosen where the contract was silent**: the article keeps a 12 pt body at 1,5 spacing with 12 pt bold headings (the journal fixes no body size for the manuscript text and «do not worry about type of font or point size»; 10 pt headings under a 12 pt body would be smaller than the text), no first-line indent (the journal forbids indents), references 10 pt; the supplement is exactly what the journal prescribes for web extra material (title/main heading 12 pt bold, headings 10 pt bold, text 10 pt at 1,15, tables 10/8/8). `JOURNAL_DOC_FLAGS` and `SUPPLEMENT_DOC_FLAGS` live in `build_journal.py`.
10. **Stubs and provisional builds**: until `prose_journal_es.py` lands, the Spanish article is built from the corpus article (optional paragraphs dropped) with the journal display items, and the report marks the build «provisional» with the stub named; `journal_config`, `prose_journal_en` and `supplement_journal` landed during this task and are used directly. Budget violations fail the build unless a stub is in use or `--no-strict` is given (the English prose is still over budget while builder A trims: 5938 words, legends 221/313/361/356).
11. **Not touched**: `common.py` (builder P), `journal_config.py`, `prose_journal_en.py`, `supplement_journal.py`, the corpus outputs (the report hashes `outputs/sin_rett/*/{figures,extra,tables}`, `values_*.json` and `tidy` before and after each build; the files that changed during this task's builds were plates re-rendered concurrently by builder P, never written by this builder, which writes only under `manuscript/journal/`).

## Task B1 — journal-mode plates (`PLATE_JOURNAL`), 2026-09-09

Read before deciding: `journal/plan.md` (sections 2, 4, 7.3, 7.4, 8, 9), `common.py` (`plate_resolve`, `save_fig`, `atomic_write_csv/json`, `check_layout`, `_pl_text_items`, `_pl_name_map`), the nine plate modules (06, 08a, 08b, 08c, 08d, 13, 14, 15, 15b: every `set_title`, panel-letter helper and `savefig` call), `supplementary_material._plate_stems`, `tests/test_supplementary_registry.py`, `journal_guidelines.md` and the official PDF text ("Use lowercase font (a, b, c…)", "Do not add box outline to graphs", "Do not use titles in the graph or artwork. Titles should appear at the beginning of the figure legend", 300 dpi / 107 mm minimum), the four corpus `captions.json`.

Decisions (each with its reason):

1. **Folder = the plan's, not the task text's.** The task text names `outputs/sin_rett/<lang>/figures/journal/`; plan.md 7.1 (`PLATE_DIR`) and 7.4 name `outputs/sin_rett/<lang>/journal/figures/`. The plan is the contract the other builders read (`journal_config.PLATE_DIR`, `build_journal --render-plates`), so the plates are written to `outputs/sin_rett/<lang>/journal/figures/`. A sub-folder of `figures/` would also have been picked up by `_plate_stems`' non-recursive glob only if named `*.png` at the top level, but the registry test's intent (journal plates invisible to it) is met either way.
2. **One flag, read in `common.py` only: `PLATE_JOURNAL`** (unset/0/off = today's behaviour; `1` = journal mode for `sin_rett`; the name of a variant selects that variant). `common.plate_journal_mode()`, `plate_journal_variant()`, `journal_redirect(path)`, `journal_strip_titles(fig)`, `journal_artwork_text(s)`, `journal_number_pass(fig)`, `journal_plate_export(fig, path)`, `journal_caption_text/entry`. No other module reads the environment.
3. **Where the mode acts.** (a) `plate_resolve`: in journal mode, right after `_pl_white_labels` and before `_pl_title_fit`, `journal_strip_titles` reduces every axes title `^\([a-f]\)\s+…` to the bare letter, blanks (to one space, so the anchored letter keeps its line) the title of an axes whose letter is a separate annotation (13/14 `ax._panel_letter`, 08b `C.letter`), empties `fig._suptitle` and hides any text equal to a variant stamp (`CFG.VARIANTS[*]["short"/"label"]`, 08a's `F84 without Rett` strings; 08d's FS_VARIANT stamp is such a text). A title with neither a letter nor a letter annotation is KEPT and logged (`kept_titles`): these are facet/strip labels and inset labels, not panel titles (in the 40 plates only two kinds survive, both audited in `panel_titles.json` under `kept_titles`: the "Mean depth" / "Profundidad media" inset label of Figure 2c, which is the inset's axis-like label and is named in the legend; and the two facet strip labels inside panel (f) of Figure S15 = `E11_rem_a03_codes_by_era` ("2024 era: 31–59 months", "2025 era: redesign"), which separate the two definition eras of one panel exactly as the strip labels of Figure 4e do). (b) `journal_plate_export(fig, path)`, called at the last line before every module's `fig.savefig` (06, 08a, 08b, 08c `save_plate`, 13, 14, 15, 15b) and inside `C.save_fig` (08c `save`, 08d): strips again (idempotent), applies the English number pass, redraws, re-runs the composition checker on the state that is written (`PLATE_CHECK` respected, `_plate_checked` reset), records the removed titles in `journal/figures/panel_titles.json`, and returns the redirected path. The export hook is the identity when the flag is off, so the eight module edits are one line each and inert.
4. **Why the hook sits at `savefig` and not only in `plate_resolve`.** Module 13 reinstalls tick formatters after `plate_resolve` (`localise_ticks`) and 13/14 run their own letter and language checks before saving; 08a/08b/08c trim context lines after `plate_resolve`. Only the final save sees the state that is printed, and `plate_resolve` does not know the language (the path does).
5. **English number pass in the artwork** (plan 7.4, 9): `(?<=\d)\.(?=\d)` → "·"; `\d{1,3}(,\d{3})+` → digits when four digits ("2,334" → "2334"), U+202F narrow no-break space when five or more ("100,000" → "100 000", "13,155" → "13 155"), the same rule as the document pass of 7.5. Protected: ICD-10 codes (`F84.0`, `J18.9`), eight-digit REM codes, `P6…`, `Law/Ley 21.545`, versions, DOI, URL, ISO dates, file names, `a:b:c` keys, SHA-256 and matplotlib mathtext (`$…$`, where log-axis powers live). Tick labels are regenerated at every draw, so a delegating `Formatter` wrapper is installed on every axis (major and minor) instead of editing tick texts; all other `Text` artists are rewritten. DejaVu Sans carries U+00B7 and U+202F. Spanish plates keep the corpus conventions (100.000; 0,5), as plan 7.5 requires for the working translation.
6. **No corpus write in journal mode.** `journal_redirect` maps `<v>/<lang>/figures/…` and `<v>/<lang>/extra/figures/…` of the submitted variant to `<v>/<lang>/journal/figures/…`, `controls/<x>` to `controls/journal/<x>`, and EVERYTHING else under `outputs/` (tables, extra/tables, tidy, and the plates of the other variant that 06/08a/08b/08d render because they have no `--variants` flag) to a scratch folder outside the repository (`tempfile.gettempdir()/study_journal_scratch`, deleted at the end of this task). The redirect is applied inside `atomic_write_csv`, `atomic_write_json`, `plate_check_write`, `save_fig` and the export hook; the printed-table and printed-label rules (`is_printed_table`, `is_printed_labels`, range-dash) are evaluated on the ORIGINAL path, so the redirected captions carry the same range-dash treatment as the corpus ones. Verified: after all journal renders the SHA-256 of every file under `outputs/` outside `journal/` equals the pre-task snapshot (1516 files, 0 changed).
7. **`captions.json` beside the journal plates** has the corpus schema `{key: {title, caption}}` plus `panels` (`{letter: title as printed on the corpus plate}`); each `(a) …` segment of the corpus caption now opens with the removed panel title ("(a) F84 per 100,000 GRD episodes. Episodes with F84 in any position …"); grouped markers `(a, b)` / `(a–c)` carry both titles joined by "; ". The figure title is the corpus title unchanged (builders strip the "Figure n." prefix per plan 2). Numbers inside the inserted titles are the ones the plate printed (computed by the module), never typed; captions are written in corpus number style so that `journal_config.journal_text` converts them once, with the rest of the document. The file is merged across modules (each module writes only the plates it rendered in journal mode in that process) and `panel_titles.json` keeps the audit record (`panels`, `kept_titles`, `removed`, `number_pass`).
8. **Rendered set.** All nine modules were run with `PLATE_JOURNAL=1 PLATE_CHECK=strict` for en and es; every plate passed the strict composition check twice (after `plate_resolve` and again after the number pass). The journal folders were then pruned to exactly the 40 keys of the plan (4 body + 36 supplement) — the legacy unregistered plates (`figS1_grd_variants`, `figS4_grd_model_sensitivities`, `figS7_a05_standardised_rates`) and the 19 dropped plates of plan section 5 rendered by 13/14/15/15b were removed from `journal/figures/` and from its two JSON files; `build_journal --render-plates` (08a–08d only) will re-create `figS1`/`figS4_grd_model_sensitivities`, which `asset_path` ignores. Every PNG is 4251 × 5787 px at 600 dpi (180 × 245 mm; journal minimum 300 dpi / 107 mm).
9. **Readability without in-graph titles** (task requirement), checked on the rendered PNGs at reduced size and at print size: every panel is identifiable from its axes and the legend. Panels whose identification now rests on the legend rather than on the axes alone: the three choropleth maps of Figure S9 (a–c; the colour-bar unit is on the plate, the source and year are in the legend), Figure 2e (year 2024 only in the legend), Figure S4 (b)/(f) forests (estimand only in the legend), EF5 (e) (2019 vs 2024 change; the legend and the year legend inside the panel carry it). No panel lost an axis label, a unit or a data annotation; the "definition break" annotations (Figure 3 d, f; E12 f), the place-of-care warnings (Figure 2d, Figure 4e, Figure S9 d) and the strip labels of Figure 4e stay.
10. **Byte-identity of the default mode**, proven by re-rendering with the flag unset AFTER the edits: 08d (4 plates + tables + tidy), 08a (24 plates, breaks table, titles/captions) and 08c for `con_rett` (14 plates, 14 tables, titles/captions): every PNG, CSV and JSON came out byte-identical to the pre-task snapshot; only the runlog/controls files differ (timestamps) and were restored from the snapshot. Tests: `python -m pytest study/tests -q` with the flag unset → 316 passed, 4 skipped, 1 failed (321 collected; the baseline at the start of the task was 300 collected, 299 passed, 1 skipped — the extra tests were added by the other builders while this task ran). The one failure, `test_fixed_panel_gloss.py::OnlyOneCopyOfTheGlossTest::test_no_other_file_of_the_package_copies_the_canonical_clause`, names a single offender, `journal/prose_journal_en.py` (builder A, written in parallel at 13:41), which copies the canonical fixed-panel clause literally instead of calling `C.fixed_panel_gloss`; no file of this task is named and the test passes on the files of this task.
11. **Incident, recorded for honesty.** The first journal run of 08c wrote its journal-style plates INTO the corpus (`outputs/sin_rett/*/figures/fig4_triangulation.png`, `figS9`–`figS15`) because 08c saves through its own `save_plate` (`fig.savefig`), which I had not hooked (I had only seen its `C.save_fig` wrapper). Detected by the hash audit, the 14 files were restored from the snapshot taken before that run (the restored files carry their original 8 Sep 22:52 timestamps and hashes), 08c was hooked, and the journal render repeated. The corpus is hash-identical to the snapshot.
12. **Not done here (other tasks).** `docx_builder.build_document(journal=…)` of plan 7.5 is not touched by this task (it belongs to the document builder); `outputs/controls/journal/plate_check_journal.json` is written by `build_journal` (plan 7.3); no test was added under `tests/` (not assigned) — the journal mode is covered by the manual verification above and by the unchanged test battery.

## Task B2 — the focused prose: `journal_config.py`, `prose_journal_en.py`, `prose_journal_es.py` (2026-09-09)

Read before writing: `journal/plan.md` (sections 1–3, 6, 7.1–7.2, 7.5, 7.7, 8), `prose_en.py` and `prose_es.py` in full, `tests/test_prose_en.py` / `test_prose_es.py` (parity conventions), `docx_builder.build_document` (block grammar), `supplementary_material.FIGURE_ORDER/TABLE_ORDER`, `equations.NUMBER`, the four `captions.json`/`titles.json` of `outputs/sin_rett`, `T2_grd_core.csv`, `T7_models.csv` + `T7_models_numeric.csv` (en and es, row-aligned), `outputs/tidy/spatial_{moran,lisa,inequality,correlations}.csv`, `references.bib`. Files written: `journal/journal_config.py`, `journal/prose_journal_en.py`, `journal/prose_journal_es.py`, `tests/test_journal_prose.py`; nothing in the corpus was modified.

Decisions (each with its reason):

1. **Section word targets adjusted in `WORD_BUDGET`; the plan's ceiling kept.** The plan's paragraph map mandates content whose own estimates sum to ≈1650 (Methods) and ≈1950 (Results) words against section totals of 1350 and 1650. After three compression passes (compact "a–b" confidence intervals, range citations for companion items, crude rates dropped where the standardised rate is printed, the survey n dropped where the cases are printed, the derived Census effect dropped) the text measures Introduction 388, Methods 1499, Results 1798, Discussion 1020, Conclusion 78 = 4783 words (`prose_en.word_counts`, before the number pass). `WORD_BUDGET` therefore carries methods = 1500, results = 1800, discussion = 1050 as the tightest attainable targets; `core_body_max` = 4800 and `core_body_min` = 3500 are unchanged and the journal's own 3500–5000 window is asserted separately in the tests. Every paragraph the plan names is present with its keys; only wording was compressed.
2. **Confidence intervals as "a–b" (en rule) in the prose**, produced by a local `_K` accessor over `prose_en._V` with `PE.fmt_number`; the plan's Summary already uses that form. Reason: the corpus "95% CI a to b" cost two words per interval across ≈30 intervals and the Summary is measured at 248 words with the compact form.
3. **Body legends and body-table notes are assembled from verbatim fragments of the corpus captions/notes** (`assemble_fragments`: every fragment must be a substring of the stored caption; connectives must carry no digit except registry labels and identifiers such as F84 or A05; computed parts are marked "=") — so every number a legend prints is a number the corpus caption prints and no number is typed by hand. Trimmed to 152 / 287 / 292 / 296 words measured on the whole printed legend, heading included (Figure 1 ≤ 160, others ≤ 300), which is how `build_journal.py` holds the caption to the caps; the 10 pt bold heading is the stored title with its variant suffix rewritten as "(F84 family excluding Rett syndrome)" and is exposed as `JournalRegistry.heading_of(key)` and in `payload["heading"]`. `legend_word_counts()` returns the same whole-caption count. The Figure 1 "HOW IT IS DRAWN" and "SOURCE OF THE FIGURES" blocks and the Figure 2 hospital name list leave the legend (they point to appendix Tables S13 and S41); the parenthetical "(not 'hospitalisations for autism')" of the corpus caption and note is not carried into the body legend/note so that the R6 grep finds the phrase nowhere in the article.
4. **`JournalRegistry` never remaps caller-supplied text.** `remap_refs` (corpus numbering → journal numbering) is applied only to captions/titles/notes read from `captions.json`/`titles.json`; a legend or note written against the registry already carries the journal numbering. `journal_text_blocks` remaps only table/figure payloads without the `refs_remapped` flag. Reason: a first build rewrote the journal "Table S13" of the Figure 1 legend as if it were a corpus number.
5. **Table cuts as presentation filters with the CSV columns intact.** `table_block` returns the whitelisted rows (25 and 29) with every column of the CSV (Table 2 without Notes); the Block/Estimand column is kept as data and named in `payload["heading_column"]` for the journal document mode (8 pt bold internal headings); `with_internal_headings()` is offered for a builder that prefers heading rows. The Spanish cut selects the same positions found in the English CSV (row-aligned outputs; asserted by shape) and the p column is rebuilt from `T7_models_numeric.p_value` in both languages (`format_p`: two significant figures, "<0·0001"; comma for Spanish).
6. **`format_p` keeps the mid-height point for English as the contract says**; the parity test normalises "·" to "." before extracting numbers (the English prose carries "·" only where `format_p` prints it; every other decimal is converted by `journal_text` at build time, after word counting).
7. **`journal_text` number pass**: mid-height point, four-digit numbers without a separator, five or more digits with U+202F; protected tokens extended beyond the plan's list with `\bP\d{7}\b` (Serie P codes such as P2500500), `v\d+\.\d+`, "Python 3.14", "version 1.0" and Spanish ordinals; the thousands pattern accepts a number followed by a list comma ("1,012, 1,218"). Verified on the rendered preview: no full-stop decimal survives outside protected tokens.
8. **Territory paragraph numbers come from the tidy tables through `tidy_value` and the added `tidy_count`** (LISA/Gi* class counts under BH rejection, N = 342 continental comunas); the Moran p is printed with `format_p` (0·0010 at 999 permutations).
9. **Coding-depth means are printed without SD** (no SD key exists in the values file; the medians exist but the journal's mean (SD) form cannot be built), so the "mean (median)" rows leave Table 1 as the plan says and the means stay in the text and Figure 2c; the hospital rate distribution is printed as median with IQR.
10. **Summary**: 248 words; "; no effect of Law 21.545 was modelled" was rephrased to "; Law 21.545 was treated as context, not modelled" (same meaning, one word longer) because the corpus's banned-substring test forbids "effect of law 21.545 was"; the plan's first trim ("in school integration programmes" → "in school integration") was applied.
11. **Spanish twin**: same block sequence, same keys per paragraph, same citation order; the data-sharing counts print the `_en` keys in both languages (the `_es` values are equal) so that numeric parity holds by construction; the tests compare the number multiset of every paragraph, legend and note between the two languages (stricter than the corpus's whole-document comparison).
12. **Author-supplied placeholders** stay bracketed and are never filled: affiliation, the second author who accessed and verified the data (the Contributors paragraph states that two authors did so), the ethics confirmation, the repository URL/DOI, the Codex version, ICMJE forms; funding is "None".
13. **Interface check against the parallel builders' files** (`build_journal.py`, `supplement_journal.py`, present on disk at the end of this task): every `journal_config` name and registry method they call exists (`heading_of` was added for `build_journal`); their legend budget counts the whole caption, which the legends now satisfy.
14. **Out of scope, left to the other builders and recorded here so the readers do not look for them in these files**: superscript citations after punctuation, no-bold-in-body, 10/8/8 pt tables, landscape tables, page numbers (docx journal flag, task P/B); journal plates without in-panel titles (task P; the previews in this task used the corpus plates through `article(V, plate_dir=...)`); the "Available from:" localisation of the reference list (`m10.localised_references("en")`, build_journal).

Verification done in this task: `python3 -m pytest tests/test_journal_prose.py` (13 tests: budgets, Summary structure and no references, panel sources/dates and no references, 30 references in the plan's order and only once for dataset keys, first-citation order ascending and complete for 36 figures and 85 tables, every supplement asset on disk, per-paragraph numeric parity en/es, table cuts byte-identical to the CSV with recomputed p, legends built only from caption numbers, standing-rule phrases absent, number pass, remap, DOCX build of both languages); previews rendered to PDF with LibreOffice and read page by page (title page with computed counts, Summary, a Results page, Table 1, Table 2, Figure 2 with its legend); `pdffonts` shows Times New Roman only.

## Task INTEGRATE — wiring the four builders, the three deliverables and the measured checklist (2026-09-09)

Read before integrating: `journal/plan.md` (sections 2–8, the 7.x contract), the four build reports, `build_journal.py`, `journal_config.py`, `prose_journal_en.py`/`prose_journal_es.py`, `supplement_journal.py`, `docx_builder.py` (journal branch of `build_document`, `nota_tabla`, `_add_runs`/`_REF_RE`), `pipeline/10_manuscript.py` (`to_pdf`), `journal_guidelines.md`, the four `captions.json`/`titles.json`, `T2_grd_core.csv`/`T7_models.csv` (en and es), `references.bib`. Built with `python3 journal/build_journal.py --lang all` three times (baseline, after the interface fixes, after the note-bold fix); every measurement was taken on the rendered PDF/DOCX (`scratchpad/measure_final.py`), never on a report alone.

Decisions (each with its reason):

1. **Interface mismatch fixed at its source — the appendix printed the body cuts.** `JournalRegistry.table_block` applied the `TABLE_ROWS`/`TABLE_COLUMNS` presentation cut whenever the key was a body table, so the supplement's "Table S37" and "Table S82" (which `supplement_journal` calls with the same registry and introduces as "the unmodified full table … of which Table 1/2 prints a selection") came out with 25 and 29 rows. The cut is now applied only when the label is a body label (`_is_body_label`: "Table n"/"Tabla n") or rows are passed explicitly; Tables S37 and S82 print 45 and 54 rows (53 and 71 DOCX rows with their internal headings). The contract signature is unchanged.
2. **Interface mismatch fixed at its source — Spanish internal headings.** `TABLE_HEADING_COLUMN` names the English columns ("Block", "Estimand"); the Spanish CSVs carry "Bloque"/"Estimando", so `docx_builder.agrupar_por_columna` found no column and the Spanish Tables 1–2 printed without the 8 pt bold block headings (26 and 30 rows against 32 and 41 in English). `table_block` now resolves the heading column by its position in the English file before the cut (`_heading_column`), so both languages print the same heading rows.
3. **Plates reach the PDF at 600 dpi.** LibreOffice's PDF export downsamples every image to 300 dpi by default ("Reduce image resolution"), so the corpus PDFs and the first journal build carried the 4251 × 5787 plates as 2125 × 2893 px (300 ppi) although the DOCX embeds them at 600 dpi. `build_journal.to_pdf` now converts with `writer_pdf_Export:{ReduceImageResolution:false, Quality:95}` (`PDF_EXPORT_FILTER`) and the review records `pdfimages -list` (`images` in the report): the four body plates are 4251 × 5787 px at 600 ppi in both articles (PDF 5·5 MB instead of 2·9 MB); the supplement plates stay at the 300 dpi they are embedded with (`--supp-dpi 300`, 44·6 MB). `m10.to_pdf` (the corpus converter) is untouched.
4. **No bold cross-references in the journal table notes.** `docx_builder._add_runs` bolds every "Table Sn"/"Figure n" token (corpus convention, `refs_bold=True`); the journal branch already disabled it for paragraphs but not for `nota_tabla`, so "Table S37", "Table S82" and "Table S83" printed bold inside the notes of Tables 1–2 (the journal forbids bold for emphasis). `nota_tabla` gained `refs_bold=True` (default = today's behaviour) and the journal call passes `refs_bold=False`; the three docx guard test files pass (47 passed, 3 skipped).
5. **SAGER: one clause added to Methods › Study design in both languages** — "reports results by sex as registered in each source — no gender variable was analysed — following SAGER". Reason: the journal requires the sex/gender variable to be defined per SAGER (and "sex assigned at birth" where applicable); no register or survey of the study records gender or sex assigned at birth, so the article states what "sex" is. This is a content edit outside the strict file list of this task, made because the rule is binding and the fix is twelve words; it is reversible by deleting the clause. Methods measures 1513 words (was 1499), the manuscript text 4797 (ceiling 4800), so `WORD_BUDGET["methods"]` moved from 1500 to 1520 (the plan's hard ceiling and floor are unchanged). `tests/test_journal_prose.py` (numeric parity per paragraph, budgets, citation order) passes.
6. **What was verified and how (journal_checklist.md).** One row per rule of `journal_guidelines.md` with the measured value: 4797 words; Summary five paragraphs, 248 words (254 tokens on the PDF); panel with the exact search date and no citation; 30 references in order, 9 with "et al" (≥ 7 authors), en rules in 19 ranges; superscripts after the punctuation (33 runs); 0 ASCII decimals outside protected tokens, 555 mid-height points; p values "<0·0001" and "0·0010"; tables 8 pt with 8 pt bold internal headings (32 and 41 rows); legends 10 pt with the bold 10 pt heading (152/287/292/296 words); figures one per page at 600 ppi, lowercase letters, no in-graph titles (the four plates read at reduced and print size); statements present, "undecided" absent; supplement 454 pages with folios 1–454, TOC of 144 entries spot-checked 16/16, equations 1–33 once each in order, 39 appendix references, Table S11 and the plan date present; R6 phrases absent from the article and only negated in the supplement.
7. **Residuals recorded, not hidden**: 22 appendix tables at 6–7·5 pt (too wide for A4 landscape at 8 pt); the "⚠" glyph set in a fallback font in the supplement PDF; the bracketed author-supplied placeholders; coding-depth means without SD (no SD key exists).
8. **results_map.md** classifies all 18 Results paragraphs (numbers traced token by token into the journal article and supplement; the seven tokens found in neither are named with their keys: annual M-CHAT-R/F totals, the five-year A05 total, the millions of the dropped coverage paragraph, the dispersion rounded differently) and all 59 figures and 132 tables (4 + 36 + 19 dropped; 2 + 82 + 48 dropped, each drop with its reason). **reference_cut.md** lists the 50 keys with kept/dropped, the journal number and the reason; the six dropped dataset/statistical keys are cited in the appendix list.
9. **Not touched**: the corpus outputs (hash audit unchanged in every build), `manuscript/*.docx|pdf` (dated 2026-09-08), `common.py`, the plate modules, `decision_log.md`, `supplement_journal.py`. Files changed by this task: `journal/journal_config.py` (decisions 1, 2, 5), `journal/build_journal.py` (3), `docx_builder.py` (4), `journal/prose_journal_en.py` and `prose_journal_es.py` (5), and the new `journal/journal_checklist.md`, `results_map.md`, `reference_cut.md`.

Tests: `python3 -m pytest tests -q` → 330 passed, 4 skipped after decisions 1–3 and 5; re-run in full after decision 4 (the `nota_tabla` flag) → 330 passed, 4 skipped, 334 collected (6 min 12 s).

## Task CLOSE ROUND 1 — the readers' open points, fixed at their source (2026-09-09)

Read before deciding: the readers' list (46 items), `journal/plan.md` (7.x contract, section 8), the archived journal text (`journal_guidelines/tlam-info-for-authors-1759484803240.txt`: title page, references — Index Medicus, «1687–[en rule]92», access date —, tables — «Legends should be in 10 point» —, «We use a comma before the final "and" or "or" in a list of items», SAGER, «the quality (risk of bias) of that evidence»), `journal_config.py`, `prose_journal_en.py`/`_es.py`, `supplement_journal.py`, `build_journal.py`, `docx_builder.py` (`plate_layout`, `plan_full_page_plates`, `split_caption_lines`, `insertar_tabla`, `insertar_ecuacion`, `pie_figura`, `is_lead_paragraph`), `pipeline/10_manuscript.py` (`localised_references`, `landscape_wide_tables`, `_fit_font_pt`, `postprocess_docx`), `common.py` (journal plate mode), `references.bib` (journal, pages, urldate of the 52 cited keys), `outputs/values_sin_rett.json` (numerator keys), the CSV headers of every table whose intro was bare, the corpus texts behind every un-remapped token, `outputs/sin_rett/en/journal/figures/{captions,panel_titles}.json`, and the previous `manuscript/journal/*.pdf` page by page where the readers pointed.

Decisions (each with its reason; «kept» marks a reader's demand that a standing rule or the journal's own rule overrides):

1. **Methodology–results consistency (blocking).** Six tables return to the appendix in first-citation order, all cited from the article text: `EF4_severity_weight` (equation 18, Jeffreys intervals of in-hospital lethality; Results › Hospital ¶4), `E14_rem_a05_age_sex` (equation 7, exact binomial count ratio; Results › Pathway ¶2), `E1_grd_seasonality` (equation 19, the GRD monthly index; Results › Pathway ¶3), `E62_grd_region_population_rates` and `E13_rem_a05_regional` (region × year, residence and place of care, never divided one by the other; Results › Territory) and `E45_bivariate_moran_pairs` (equation 26; Results › Territory, with the standing-rule wording «a spatial co-location of unlinked systems, not a direction or a linkage»). The E45 PLATE stays dropped: the table prints the estimator's output, which is what the methodology promises; the scatter invites the cross-source reading the standing rule forbids. `journal_config.SUPP_TABLES` is 91 (`RESTORED_TABLES` names the six), every later S-number moves, and the tests' fixed counts follow. `results_map.md` was regenerated from the registry (old → new S-numbers), never by hand.
2. **Reference form = the journal's own example**, in the journal mode of `pipeline/10_manuscript.py` (`journal_reference_style`, applied inside `localised_references(journal=True)`; default corpus behaviour untouched): Index Medicus abbreviations from a 24-name map that covers every journal met in the article and the appendix list (`JOURNAL_UNABBREVIATED` stays empty), «Lancet 2007; 370: 1453–57» (no issue number, en rule, end page shortened to the digits that differ, «e1001885»/«h1961» untouched), and «(accessed Sept 4, 2026).» after every URL from the .bib `urldate` (Spanish: «consultado el 4 de septiembre de 2026»). The reader's «full end pages and issue numbers differ from the journal's example form» is therefore met by adopting the example form rather than plain Vancouver.
3. **Percentages with their n, means with SD.** Every % the readers named now carries its numerator from a values key: Summary and Hospital ¶1 «95·5% (8341 episodes)» / «8341 of the 8731 episodes» (`grd_f84_secondary_only_n_2024`, `grd_f84_any_n_2024`), Sources «1 048 706 of the 1 085 813 episodes (96·6%)» (`grd_records_fixed65_2024`, `grd_records_total_2024`), DEIS «581 of them (85·7%)» (`deis_f84_principal_snss_2024`), ENDIDE «129 cases among 2880 surveyed» / «34 of 2646» (`svy_endide_children_reported_{male,female}_{cases,n}`). The top-decile shares of the Territory paragraph (25·7% → 20·6%) stay without a numerator: the tidy table carries the share only and a count would be hand-derived (standing rule). The coding-depth means (4·39 → 5·78) leave the text because no SD is computed: the sentence now prints the medians («four in 2019 to five in 2024», `grd_coding_depth_all_median_*`) and points to the mean-depth inset of Figure 2c; no IQR key exists either, so no IQR is invented.
4. **Title page** per the journal rule: bracketed, author-supplied placeholders for the preferred degree, the full postal address of the affiliation, and the corresponding author's postal address and telephone (never invented); the build stamp and the word counts leave the submission title page (they stay on the Spanish working translation, `build_journal.title_page`). Contributors: the second-author sentence remains a bracketed placeholder — the journal's «more than one author accessed and verified» rule cannot be met by inventing an author; «The corresponding author had full access…» (Methods) now agrees with «All authors had full access» (Contributors).
5. **Research in context** gains the quality (risk of bias) of the evidence in one sentence, as the journal asks; still no citation. **SAGER**: Methods defines sex («the sex recorded by each register or declared to each survey; no source records sex assigned at birth or gender») and Limitations carries the consequence; the term «sex assigned at birth» is used to say that no source records it, which is the truthful use.
6. **House style** — comma before the final «and»/«or» of every list (62 hand edits in `prose_journal_en.py`, legend and note fragments split so that every fragment stays a verbatim substring of the corpus text; a numeric-list rule in `journal_config._HOUSE_STYLE` for the series the corpus formatter writes, «65, 65, 68, and 72»); «eg,»/«ie,» without stops (the «. referrals» the readers saw was «e.g. referrals»); the corpus's «⚠» becomes «†» so the supplement is Times New Roman only (`_HOUSE_STYLE`; the notes that explain the glyph now explain the dagger); «Fable 5.1» and the REM dictionary file names (`.xls`, `.xlsx`, `.xlsm`, `.zip` …) join the protected tokens; the Figure 3 legend sentence opens in sentence case.
7. **Range dashes never open a line**: WORD JOINER on both sides of every digit–dash–digit range in paragraphs, legends, titles and notes of both languages (`journal_config.tie_ranges`, the device `docx_builder.compound_indivisible` already uses for «Getis–Ord»). **Kept for table cells**: the number-width guard of the tables measures the atoms on either side of a dash; tying them would force mid-number breaks in narrow columns, so the readers' cell cases (Tables S19, S38, S53 …) remain and are recorded as such.
8. **Plates.** All 36 supplementary plates are now the journal-mode renders (no in-graph titles, journal number style) read from `outputs/sin_rett/<lang>/journal/figures/` — `JournalRegistry.fig_path` and `asset_path` require the journal PNG and refuse the corpus one — and their legends are the journal captions that re-insert the removed panel titles. `common.py` journal mode (flag only): a white, borderless backing under every frameless legend and under a panel note that lies over a context line (the dotted Law 21.545 line) and over no data (`journal_back_legends_and_notes`, applied in `journal_plate_export`); «con_rett − sin_rett» and the variant code names are rewritten in the artwork (`journal_label_text`) and in the captions (`legend_label_text`). Re-rendered: 08a–08d through `--render-plates`, then 13, 14, 15 and 15b in journal mode; SHA-256 of every corpus file outside `journal/` and `controls/` unchanged (audited before and after). **Kept**: the Figure 3f note («n primary care 482–1274 · specialty 95–153») is not boxed because it also lies over a data line and covering data is worse than a context line; the bold strip/inset/group labels of Figures 2c, 3a–b and 4c/e stay (axis-like labels, audited in `panel_titles.json`), as the plan decided.
9. **Cross-references**: `remap_refs` is case-insensitive («table S4»); dropped duplicates map to their kept twin (`DROPPED_ALIASES`, e.g. corpus «Table S27» = `F3_rem_pathway_data` → the journal Table of `T3_rem_pathway`); legacy references that no registry number resolves are substituted per key and field with the journal labels masked while the registry remap runs (`LEGACY_REFS`, «Companion table of plate EF6.» → «Companion table of Figure S8.»; `JournalRegistry._remap`), so nothing is remapped twice.
10. **Document typography** (journal flags of `docx_builder.py`, defaults byte-identical): table notes at 10 pt (`table_note_pt`, the journal's «Legends should be in 10 point»); no table below 8 pt (`table_min_pt` in `insertar_tabla` and in `landscape_wide_tables`/`_fit_font_pt` through `postprocess_docx(min_table_pt=…)`; a wide table now wraps its long tokens instead of shrinking — the supplement grows accordingly); the supplement text single-spaced; equation rows `cantSplit`; internal table headings keep with their first row; the bold legend heading carries its full stop (the «syndrome) .» the readers saw was a pdftotext artefact at the run boundary); `split_caption_lines(hard_spaces=True)` no longer turns «100 000» into «100 000»; and the document that ends with a plate no longer prints a blank last page (`_absorb_trailing_section`).
11. **Plate pages**: in journal mode a plate may shrink to `plate_min_w_cm` = 140 mm (journal minimum 107 mm; 4251 px at 140 mm = 770 dpi) so that its whole legend — or at least three lines of it — sits under the image (`plate_layout(min_w_cm, min_lines)`), the article keeps one figure per page and the supplement lets a residual tail be followed by the next unit; the presentation paragraph of the appendix rides with its plate whatever its length (`is_lead_paragraph(pt, max_cm)`, `lead_max_cm` = 6 cm at 10 pt) — without this the longer «what it adds» paragraphs were left alone on a page in front of their plate. This is what removes the readers' seven near-blank legend pages and puts the TOC page on the plate.
12. **Supplement text**: the front matter reconciles the analysis plan's primary case definition (full F84 family) with the submitted variant and states the 87-episode difference; Part A introduces Tables S1–S10 by number; every table intro now says what the table adds (the 22 bare «carries the values drawn in» sentences were rewritten from the CSV headers); the promise «each table by a sentence that states what it holds and what it adds» matches; a new section B12 names every plate and table of the full corpus that is not printed, with its corpus title, and the article's data sharing statement names the full set (59 figures, 132 tables) and how many are printed — sizes read from the registries; «A10. Appendix references» is a labelled heading; the p columns of Tables S88, S89, S76 and S77 come from their numeric companions through `format_p` («0·0010», never «< 0.001»). **Kept**: the original papers of Getis–Ord, Durbin–Watson, Theil/Gini and M-CHAT-R/F are not added — `references.bib` holds only verified entries and the appendix text that would cite them is corpus text printed verbatim.
13. **Budgets**: the readers' demands add about 150 words (n beside %, SAGER, panel quality, restored citations); `WORD_BUDGET` moves to results 1900, methods 1560, discussion 1120, panel 500, ceiling 4980 — under the journal's 5000. Measured: Summary 249, panel 479, manuscript text 4945 (`prose_en.word_counts`, before the number pass).
14. **Not fixed, with the reason and the cost**: the bracketed author-supplied placeholders (ethics confirmation, ICMJE forms, repository URL/DOI, Codex version, second author, degree/addresses/telephone) stay visible — filling them would invent facts (plan R8); the missing original references of the appendix estimators (cost: verifying and adding four entries to the bib and editing corpus methodology text); dash breaks inside table cells (cost: mid-number breaks); the Figure 3f note over a data line (cost: hiding data); the supplement's growth from 454 to about 620 pages, the price of the 8-pt floor on wide tables (cost of the alternative: splitting or transposing corpus tables).

Files touched: `journal/journal_config.py`, `journal/prose_journal_en.py`, `journal/prose_journal_es.py`, `journal/supplement_journal.py`, `journal/build_journal.py`, `journal/results_map.md`, `journal/reference_cut.md`, `journal/journal_checklist.md`, this file; shared builders behind their journal flags only: `docx_builder.py`, `pipeline/10_manuscript.py`, `common.py`; tests `tests/test_journal_prose.py`, `tests/test_supplement_journal.py` (counts and the alias/tie expectations). Nothing in `outputs/` outside `journal/` and `controls/journal/` changed (hash audit); `manuscript/*.docx|pdf` untouched.

Verification of this task (on the rendered files, 2026-09-09 16:02–16:15): final build `python3 journal/build_journal.py --lang all --render-plates` (08a–08d re-rendered in journal mode, rc 0, `corpus_unchanged` true) followed by one supplement rebuild for the EF4 title; `article_sin_rett_en.pdf` 22 pages (4945 words of manuscript text; Summary 249; panel 479; 30 references in the journal form with access dates; Times New Roman faces only; 0 ASCII decimals outside protected tokens; 0 lines opening with a range dash; table notes and legends 10 pt; Tables 1–2 at 8 pt with their headings kept with their rows; Figures 1–4 one per page with the whole legend on the page, plates 180/163/157/154 mm at 600 ppi; no blank page); `supplement_sin_rett_en.pdf` 569 pages (TOC of 151 entries verified on the PDF, folio on every page, text single-spaced, 82 tables at 8 pt and 9 at 7 pt by the documented exception, 91 notes at 10 pt, 36 journal plates each with its legend heading on its page and 15 labelled «(continued)» tails, 33 equations once each in order, «†» for the corpus warning glyph, 0 «< 0·001» cells, no fallback font, 3 short non-plate pages of 420–650 characters); `article_sin_rett_es.pdf` 23 pages with the same numbers per paragraph. Tests: `python3 -m pytest tests -q` → 330 passed, 4 skipped, 121 subtests (6 min 06 s), then the two journal test files again after the last registry change (34 passed). The measured values are in `journal_checklist.md` (rewritten from `scratchpad/gather.py`), the item map in `results_map.md` (S-numbers regenerated from the registry) and the reference decisions in `reference_cut.md`.

## Task SUPPLEMENTARY COMPLETENESS reader — independent audit of `supplement_sin_rett_en.pdf` against the full corpus (2026-09-09)

Read: `manuscript/journal/supplement_sin_rett_en.pdf` (569 pages; `pdftotext -layout`, pages 15–16, 21–22, 265 and 358 also rendered and read as images), `article_sin_rett_en.pdf`, the full `manuscript/manuscript_sin_rett_en.pdf` (Results text, 59 figure and 132 table headings, the corpus intros of the 19 dropped plates), `journal/results_map.md`, `journal/journal_checklist.md`, `journal_config.SUPP_FIGURES/SUPP_TABLES`, `equations` (33 equations, 26 estimators), the supplement DOCX paragraph sequence. Nothing was changed in any deliverable or module; this entry records the verdict only.

Verified on the rendered files: (1) Eq. 1–33 printed once each, in order, on pp 15–22, each under an estimator heading that cites its number; Table S5 (26 estimators, status read from disk, the weighted kappa marked pre-specified with the reason) and Table S6 (16 pre-specified sensitivities) present; every estimator the article uses is described (equations 6, 7, 17, 18, 19 and 21 are used by the Results and the appendix tables although the article's Methods paragraph points only to 1–5, 8–16, 20 and 23–33). (2) Independent trace of the 1401 unique numeric tokens of the full Results into the article and the supplement: the 11 tokens not found verbatim are the ones `results_map.md` names (M-CHAT-R/F annual totals 19 632 / 25 074 / 19 289, the five-year A05 total 39 771, the coverage millions printed rounded or as December counts in Table S25, the dispersion 355·7 printed as 355·73). (3) Every one of the 19 dropped plates and 42 dropped tables re-examined against the corpus Results and intros: no Results sentence rests on a dropped item without a kept substitute; the two drops a reviewer could query (E16 A27/A28 by region; the EF2/EF3/EF7 hospital-detail set) are descriptive and defensible. (4) All 36 figures carry an intro of 57–122 words (unit, denominator, why it matters, what it adds); all 80 Part B tables carry the what-it-holds/what-it-adds sentence; Tables S1–S10 are introduced by number in the Part A opening paragraph and descriptively in the verbatim methodology. (5) First-citation order of the article, ranges expanded, ascends 1…36 and 1…91; every S-item exists and is cited. (6) All 151 TOC page numbers and table titles match the pages. (7) Table S11: every 'Where reported' names an existing article heading or S-item. (8) No stale corpus number ("Figure 5", "Table 3–8", "Figure S37+", "Table S92+") in either PDF. (9) A plate image at ≥ 300 ppi sits on each of the 36 figure pages.

Gaps recorded (none blocking): the M-CHAT-R/F annual totals (children screened) are printed nowhere and are only derivable by summing rows of Table S52; the article's Methods equation pointers omit 6, 7, 17–19 and 21; three corpus titles in section B12 embed corpus numbers ("Figure 4", "Figure S15", "plate EFn") without a note that they are corpus, not journal, numbers; Tables S3, S5, S7, S9 and S10 are not named by number in the paragraph immediately before them.

## Task VERIFY-REFERENCES (independent references reader, 2026-09-09)

Read on the built `manuscript/journal/article_sin_rett_en.pdf` (22 pages, built 16:06) and its DOCX, against `journal_guidelines/tlam-info-for-authors-1759484803240.txt` (References section), `references_verification.csv`, `references.bib` and `journal/reference_cut.md`.

1. Count and order: 30 numbered entries; first appearance in the text runs 1–30 in order (33 superscript runs read from the DOCX `w:vertAlign`); every entry is cited; nothing cited in the Summary or in the Research in context panel. 26 runs follow a punctuation mark; seven sit on the cited noun with no adjacent punctuation (refs 8, 11–12, 21–22, 24, 25, 26, 29), which the journal's rule («after any punctuation mark») permits — not changed.
2. Author rule: 11 entries with six or fewer authors list all; nine with seven or more print the first three and «et al» (1, 2, 5, 6, 7, 8, 9, 12, 30) — verified against the .bib author counts. Journal abbreviations are the NLM forms. Page ranges use U+2013 and the journal's shortened end page. Twelve online sources (10, 14–24) carry «(accessed Sept 4, 2026)».
3. One formatting failure, reported (not fixed: no file assigned to this task): reference 27 (Iezzoni 1992) prints «JAMA. 1992;267(16):2197–2203.» instead of «JAMA 1992; 267: 2197–203.» in both articles. Cause: `pipeline/10_manuscript.py::journal_reference_style` matches `r"\. " + name + r"\. (\d{4});…"`, so a title ending in «?» (the only one among the 30) never reaches the journal form and the fallback `replace(". JAMA. ", …)` is a no-op. Fix at source: accept `[.?!]` before the journal name in that regex (journal-only path; default behaviour untouched) and rebuild the two articles.
4. Verification: all 30 kept keys are `status = audited` (accessed 2026-09-04) with a verification URL; the two `unverified` rows of the CSV are uncited candidates (Wynn, Preen). `reference_cut.md` gives a reason for each of the 20 drops; the six dataset/method keys it relocates are present in the appendix list (A10, refs 7, 10, 11, 13, 14, 34 of 39).
5. Cut judged: no dropped reference is one the journal or a reviewer must have. Two drops I would reverse if the author can spare a slot (candidate to release: `heidari2016`, since the SAGER statement can stand without a citation): `fyfe2026` (BMJ 2026, time trends in the male:female ratio of autism incidence — the comparator for the falling ratio that the Summary, Results and Discussion headline; the article now supports that finding with `loomes2017` alone) and `supersalud_isapre` (the 12·5 % ISAPRE share printed in the Introduction is computed from a source cited only in the appendix). Not reversed: `cid2024` (the «designed for payment» sentence is generic), `santomauro2025`, `paula2011`, `fombonne2016`, `becerril2011`, `minsal2011`, `irarrazaval2023`, `wolter2007`, the three INE/FONASA/REM-20 sensitivity layers, `lai2020`, `hull2020`, `romanurrestarazu2018`, `cid2016`, `breinbauer2022`, `acevedo2025`.
6. Minor, for the copy-editor rather than blocking: titles of refs 3, 5, 12, 27–30 keep source Title Case while the journal example is sentence case; refs 19, 21, 22, 24 omit the city of the publisher; refs 14–23 list file names inside the reference and are long; ref 10 carries two URLs.

## Task CLOSE ROUND 2 — the readers' second list, fixed at the source (2026-09-09)

Read before deciding: the readers' 27 items, `journal/plan.md` (7.4–7.6, R4–R6), the archived journal text (figures: «minimum resolution of 300 dpi, width 107 mm», «Legends should be in 10 point, single spaced», «Do not use titles in the graph»; tables: «8 point … single spaced», «Numbers in text and tables should always be provided if % is shown»; references: the «Lancet 1998; 351: 1687–92» example), `docx_builder.py` (`numeros_de`, `_anchos`, `insertar_tabla`, `plate_layout`, `plan_full_page_plates`, `build_document`), `pipeline/10_manuscript.py` (`journal_reference_style`, `_min_widths_cm`, `landscape_wide_tables`), `common.py` (`journal_strip_titles`, `PLATE_FS_FLOOR`), the CSV shapes and longest tokens of every table the readers named, `outputs/values_sin_rett.json` and the tidy tables for every numerator, and the round-1 PDFs page by page where the readers pointed (S45 p 256, S80 p 409, S89 p 448, the article's figure pages).

Decisions (each with its reason; «kept» marks a demand a standing rule or the journal's own rule overrides):

1. **Plates never shrink (blocking item on Figures 2–4 and 24 appendix plates).** The plate keeps its natural 180 mm (`plate_min_w_cm` = 18 in both documents), so `common.PLATE_FS_FLOOR` (6 pt at 1:1) is the smallest text at print size — measured 6·0 pt on every plate of the three PDFs — and a legend that does not fit under the plate continues on the next page labelled «(continued)» (the existing machinery; plan 7.5). Cost, recorded: the article's Figures 2–4 each leave a tail page (about 8 lines), and in the appendix the presentation paragraph rides with its plate only when three legend lines still fit under it (`plan_full_page_plates`, journal no-shrink rule); otherwise it closes the previous page, which is why five section openings (B2, B5, B8, B9, B10) and a few legend tails print on partly blank pages. The alternative the readers offered — raising the plate font floor to 6·0 × 180/width and re-rendering — was not taken: a 7 pt floor changes the layout of every dense panel and the strict composition check of 40 plates would have to be re-cleared for a cosmetic gain.
2. **Tables (four blocking items: S45 lethality, S89 intervals, S80 correlations, S82 «<» alone; nine 7 pt tables; words broken in a dozen tables; ranges split at the dash in 410 + 690 cells).** Three things, none of which alters a cell: (a) `docx_builder.set_journal_atoms(True)` — in journal mode the number-width guard measures the pieces LibreOffice cannot break under UAX #14 («29 587/1 151 475» with its thin thousands spaces and slash, «(−20·1» with its sign, a WORD-JOINER-tied «30·0–42·1» or «2019–2024»), where the corpus rule split at the slash, the sign and the dash and so under-measured the column; the default is byte-identical (`tests/test_table_numbers_unbroken.py` passes untouched); (b) `journal_config.journal_text(cells=True)` ties every digit–dash–digit range of an English cell and puts U+200B break opportunities inside machine tokens of 25 characters or more (paths, URLs, code lists), and the two width functions (`docx_builder._palabras`, `10_manuscript._min_widths_cm`) split words at U+200B as LibreOffice does — identity for corpus text, which carries none; (c) `journal_config.TABLE_LAYOUT` / `restructure_table_block`, applied by `build_journal.build_supplement` after the print glossary: a table wider than the 24·7 cm landscape page at 8 pt is printed in parts («Table Sn (part i of m)», key columns repeated, note under the last part, one TOC entry), the estimand of Tables S88–S89 becomes the bold internal heading of each run of rows, their free-text notes become lettered footnotes (a sentence present in every row is stated once) and a running «#» identifies the row across parts. Measured on the rebuilt appendix: 111 table blocks (18 tables in parts), all 111 at 8 pt and none below, 0 lines opening with a range dash (40 in round 1), 0 lone «<», the Table S45 lethality row «29 587/1 151 475; 2·57 (2·54–2·60)» intact, «−0·08» intact in S80, the S89 intervals intact, and none of the words the readers saw broken («Comu|na», «SH|A-256», «Denominato|r», «Durbin–Wat|son» …) on the PDF. Table S82's p column is rebuilt from `E48_rank_stability_numeric.csv` through `format_p` (`P_VALUE_NUMERIC`). Every table row is `cantSplit` and the last two rows carry keepNext (`table_rows_cant_split`), which closes the split rows of Tables 1–2 and of the appendix and removes the orphan-row pages; the appendix falls from 569 to 491 pages.
3. **n beside every % (Introduction and Results).** Every percentage the readers listed now prints its numerator and base from a named key or tidy row (FONASA/ISAPRE and INE as millions to one decimal; Census 18 480 432 of 20 086 377; NANEAS; June cut 172 against 1919; physician-confirmed 139 of 153; PIE 47 551 of 473 006 and 11 877 of 385 995; the seven JUNAEB proportions as unweighted cases/respondents). Two documented derivations join the two of the plan: (3) the GRD age-band numerators of 2024 are sums of the sex × five-year cells of `outputs/tidy/grd_age_sex_year.csv` (observed panel, any position, all activity) over the episodes with a known age, which is the base of the share keys, asserted equal to `grd_f84_any_share_age_{0_9,20plus}_2024` to four decimals before anything prints (4167 and 1004 of 8728); (4) A05 aged 0–9 in 2025 = `a05_autism_entries_both_0_4_2025` + `a05_autism_entries_both_5_9_2025` (6143 of 13 155), asserted against its share key. **Kept**: the top-decile shares print their base (2332 and 8731 episodes with a comuna of residence, `total_count` of `spatial_inequality.csv`) and not a numerator, which is not a tidy value and is never hand-derived. The Discussion recaps keep the percentages whose n Results now prints.
4. **Median without IQR.** The pipeline computes no IQR or SD for coding depth (`grd_coding_depth_year.csv` carries strata, not quartiles), so the sentence prints neither a bare median nor a bare mean: it points to the mean-depth inset of Figure 2c, whose legend now says «means only; no dispersion is drawn».
5. **In-graph titles.** The Figure 2c inset title «Mean depth» is removed in journal mode (`common.JOURNAL_TITLES_TO_LEGEND`; the legend already names the inset); the facet titles of Figure S15f print in regular weight so that they read as labels; the S17 letters no longer overlap the «100» tick because a letter-anchored title is now made invisible instead of blank (the letter of modules 13/14 is anchored to the title box). **Kept**: the lane headings of Figure 1, the era headings of Figure 3a–b and the group/strip headings of Figure 4c/e are row-group and lane labels, as the plan decided.
6. **Reference 27 (now 26)**: `journal_reference_style` accepts «.», «?» or «!» before the journal name (journal path only); Iezzoni prints «JAMA 1992; 267: 2197–203.» in both articles; page ranges of the reference lists are tied at the dash in journal mode (one appendix reference broke «101–|33» before).
7. **References**: the readers' recommendation is adopted — heidari2016 leaves (the SAGER sentence of Methods stands as «following the SAGER guidelines», without a citation) and fyfe2026 (BMJ 2026, the population-based birth cohort documenting the falling male:female ratio) is cited in Discussion ¶3 beside loomes2017; 30 references, in order; `supersalud_isapre` stays in the appendix list (no 31st slot).
8. **Summary**: the Law 21.545 sentence of Methods (repeated in the Methods section and in Interpretation) is removed: 241 words before the number pass, 249 on the PDF text with the labels whether a thin-spaced thousand counts as one word or two.
9. **Supplement line spacing**: `aux_line_spacing` = 1·0 for the supplement (subtitle, authors, presentation paragraphs, headings, references, panel): one paragraph at 1·15 remains (the subtitle line's italic style), 55 530 at single.
10. **Length**: the numerators cost about 118 words; six Results passages were compressed (the depth sentence, the JUNAEB tail, the region × year sentence whose tables the preceding list already cites, the benchmark sentence, one pointer, the index sentence) and the Discussion recaps trimmed; 4977 words of manuscript text (Introduction 388, Methods 1534, Results 1908, Discussion 1069, Conclusion 78), `WORD_BUDGET.results` = 1920, ceiling 4980 unchanged. The article prints on 31 pages because the body follows the flags exactly (12 pt, 1·5 spacing, 2·5 cm margins) and each figure keeps its own page with its tail.
11. **Not fixed, with the reason and the cost**: the bracketed author-supplied placeholders (second author, degree, addresses, telephone, ethics, ICMJE forms, repository URL/DOI, Codex version) — filling them would invent facts (plan R8); the partly blank pages that 180 mm plates leave (item 1) and the one note-alone page of Table S41 (a note kept together; splitting notes was the round-1 defect); the Figure 3 band between rows, the Figure 3f note over a data line, the truncated row labels of Figure S36d and the Figure S16f annotation (per-plate edits in modules 15 and 08b for a cosmetic gain, with the strict check to re-clear).

Files touched: `journal/journal_config.py`, `journal/prose_journal_en.py`, `journal/prose_journal_es.py`, `journal/supplement_journal.py`, `journal/build_journal.py`, `journal/results_map.md`, `journal/reference_cut.md`, `journal/journal_checklist.md`, this file; shared builders behind their journal flags only: `docx_builder.py` (journal atoms, `_palabras`, `cant_split`, `aux_line_spacing`, the no-shrink lead rule, reference ties), `pipeline/10_manuscript.py` (reference regex, U+200B-aware widths), `common.py` (invisible letter-anchored titles, regular-weight kept titles, `JOURNAL_TITLES_TO_LEGEND`). Journal plates re-rendered in journal mode: 08a–08d through `--render-plates`, then 13 (EF5, EF6, EF8, EF10) and 14; SHA-256 of every corpus file outside `journal/` and `controls/journal/` unchanged before and after each render and each build (`corpus_unchanged` = true). `manuscript/*.docx|pdf` untouched (timestamps of Sept 8).

Verification of this task (on the rendered files, 2026-09-09 20:38–21:05): `article_sin_rett_en.pdf` 31 pages (4977 words; Summary 241/249; 30 references, fyfe2026 as ref 29, Iezzoni in the journal form; Times New Roman faces only; 0 ASCII decimals outside protected tokens; 0 lines opening with a range dash; plates 180 mm at 600 ppi, smallest artwork text 6·0 pt; 74 of 74 table rows unsplittable; the near-empty page is the labelled tail of Figure 2); `supplement_sin_rett_en.pdf` 491 pages (TOC of 151 entries verified, folio on every page; 111 table blocks at 8 pt, 18 tables in parts, 0 dash-opening lines, 0 lone «<», 0 «< 0·001»; 36 plates at 180 mm and 300 ppi with every legend head on its plate page; Eq. 1–33 in order; line spacing 55 530 single / 1 at 1·15); `article_sin_rett_es.pdf` 35 pages with the same numbers per paragraph. Pages read as images: article pp 20, 26, 27; appendix pp 151 (S2 plate), 233, 241 (S45), 274 (S17), 398 (S80), 433 and 455 (S89 parts), 54, 61, 83, 93 (S16, S21 parts). Tests: `python3 -m pytest tests -q` → 330 passed, 4 skipped, 121 subtests (6 min 08 s); the two journal test files and the two number-guard files also run alone (47 passed, 3 skipped). Measured values in `journal_checklist.md` (rewritten from `scratchpad/write_checklist_r2.py` over `gather.py` and `measure_r2.py`), the item map in `results_map.md` (section F), the reference decisions in `reference_cut.md`.

## Task JOURNAL COMPLIANCE READER (independent, 2026-09-09, measured on manuscript/journal/article_sin_rett_en.pdf and supplement_sin_rett_en.pdf; nothing built or edited)

1. Length: Introduction → end of Conclusion on the rendered PDF, superscript citation digits removed, is 5033 words splitting on ASCII whitespace ("100 000" one token; DOCX gives 5024) and 5106 splitting on any Unicode whitespace; 4960 without the 73 words of headings; 4895 without the Role of the funding source, Ethics and AI-in-research paragraphs (138 words). Any straightforward count of the rendered text is 24–106 words above 5000; the ceiling is met only by the narrowest definition. Recommendation: trim about 120 words or rely on "around".
2. References: 30, first-citation order 1…30 sequential, superscripts after punctuation (five mid-sentence citations follow a word, permitted), 10 entries with ≥7 authors print three names + et al, all page ranges use the en rule; hyphens remain only inside verbatim dataset titles (refs 16, 19, 20).
3. Summary: 5 paragraphs, 241 words (248 on the Unicode split), no citations; three survey percentages in Findings carry no n. Research in context: three headings, "Sept 4, 2026" as the search date (no explicit start date; "without date restrictions"), no citations, 497 words with headings.
4. Contributors: the "more than one author accessed and verified" sentence rests on "[Second author, to be named before submission]"; with one named author the rule is not yet met. 11 bracketed author placeholders remain in the article (degree, affiliation, two postal addresses, telephone, ethics confirmation, second author ×2, ICMJE note, repository URL/DOI, Codex version).
5. Figures: four plates 4251 × 5787 px at 600 ppi = 180 × 245 mm; lowercase panel letters; no box; each plate on its own page (pp 25, 26, 28, 30) with the legend tails of Figures 2–4 alone on pp 27, 29, 31; heading 10 pt bold, legend 10 pt at 11·5 pt pitch. Bold in-graph group headings remain in Figure 3a–b (era labels), Figure 4c (survey groups), Figure 4e (strip labels) and the lane titles of Figure 1 — row-group labels, but exposed to the "no titles in the graph" rule. Maps (supplement Figures S29–S30) use Albers equal-area (pipeline/08c_figures_triangulation.py:1010).
6. Tables 1–2: 10 pt bold heading, 8 pt body at 9·2 pt pitch, 8 pt bold internal headings, notes 10 pt, n beside %. Mid-height decimals throughout (ASCII points only in Law 21.545, F84.x, version and DOI strings); numbers one to ten in words; bold only in headings; median with IQR; p values 0·0010, 0·016, <0·0001. The mean coding depth of Figure 2c (inset) is drawn without SD (legend says so): the only means-without-SD instance.
7. Supplement: one PDF, 491 pages (the earlier integration note said 454), Contents pp 1–4 with sample TOC entries verified on the page (23/24; the miss was a wrapped line; Table S30's folio sits on a leader-only line), folio on every page, text 10 pt TNR at 11·5 pt pitch, main heading 12 pt bold, headings 10 pt bold, tables 8/8-bold, 36 plates at 300 ppi × 180 mm each with its legend heading on the same page, equations as 600-ppi images 50–104 mm wide, Eq. 1–33 once each, Table S11 (STROBE/RECORD) p 29, analysis plan A9 (English rendering) p 32, A10 appendix references p 488. Spanish original of the plan is a separate .md file (journal asks for English material).
8. Cosmetic: a visible gap before en dashes in justified legends/notes where U+2060 word joiners surround the dash — article p 26 ("2019 –2024", "1 –5") and 29 places in the supplement (e.g. "2020 –2021").
9. Journal tests: 35 passed (pytest -k journal). No file other than this log was modified.

## 2026-09-09 — La atadura del rango: tres construcciones y una medición que corrigió dos veces el criterio

El lector de composición marcó «hueco visible antes de la raya» en el artículo y en 29 lugares del apéndice.
Se construyó y se midió cada alternativa sobre el PDF, no sobre la teoría:

| variante | rayas que abren renglón | huecos visibles | veredicto |
|---|---|---|---|
| U+2060 a ambos lados (ronda 2) | 0 | sí: 1·49 pt en el art. p 26, leído a 400 ppp como «2019 –2024» | rechazada |
| raya desnuda | 3 (EN p 12, ES p 14 y p 33) + 37 en el apéndice | 0 | rechazada |
| U+2060 sólo antes | 0 | sí, hasta 1·49 pt | rechazada |
| **U+FEFF sólo antes** | **0** | **0** (lo que el detector cuenta a ≥0·5 pt es el lateral del glifo; a 4× la raya imprime junta) | **adoptada** |

Dos premisas mías resultaron falsas y quedan anotadas para que nadie las repita: (1) la clase «break after» de
Unicode NO impide que LibreOffice corte ANTES de una raya desnuda entre dígitos; (2) el problema no era de qué
lado poner la atadura sino QUÉ carácter: U+2060 es punto de estiramiento de la justificación y U+FEFF no, con
idéntico efecto sobre el corte (barrido de 16 posiciones de renglón: 3 huecos de hasta 1·21 pt contra ninguno).

El mismo carácter se corrigió en `docx_builder.compound_indivisible`, que tenía el defecto latente en
«Gauss–Newton» y sus pares. Los doce documentos del corpus principal conservan U+2060 incrustado hasta su
próxima reconstrucción; el paquete de revista ya se construyó con la regla nueva.
